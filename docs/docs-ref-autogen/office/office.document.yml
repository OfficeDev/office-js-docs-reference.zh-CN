### YamlMime:UniversalReference
items:
  - uid: office.Office.Document
    summary: <span data-ttu-id="e7222-101">表示与外接程序交互的文档的抽象类。</span><span class="sxs-lookup"><span data-stu-id="e7222-101">An abstract class that represents the document the add-in is interacting with.</span></span>
    remarks: '<span data-ttu-id="e7222-102">**主机**: Access、Excel、PowerPoint、Project、Word</span><span class="sxs-lookup"><span data-stu-id="e7222-102">**Hosts**: Access, Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.Document.addHandlerAsync
      - office.Office.Document.addHandlerAsync_1
      - office.Office.Document.getActiveViewAsync
      - office.Office.Document.getActiveViewAsync_1
      - office.Office.Document.getFileAsync
      - office.Office.Document.getFileAsync_1
      - office.Office.Document.getFilePropertiesAsync
      - office.Office.Document.getFilePropertiesAsync_1
      - office.Office.Document.getMaxResourceIndexAsync
      - office.Office.Document.getMaxResourceIndexAsync_1
      - office.Office.Document.getMaxTaskIndexAsync
      - office.Office.Document.getMaxTaskIndexAsync_1
      - office.Office.Document.getProjectFieldAsync
      - office.Office.Document.getProjectFieldAsync_1
      - office.Office.Document.getResourceByIndexAsync
      - office.Office.Document.getResourceByIndexAsync_1
      - office.Office.Document.getResourceFieldAsync
      - office.Office.Document.getResourceFieldAsync_1
      - office.Office.Document.getSelectedDataAsync
      - office.Office.Document.getSelectedDataAsync_1
      - office.Office.Document.getSelectedResourceAsync
      - office.Office.Document.getSelectedResourceAsync_1
      - office.Office.Document.getSelectedTaskAsync
      - office.Office.Document.getSelectedTaskAsync_1
      - office.Office.Document.getSelectedViewAsync
      - office.Office.Document.getSelectedViewAsync_1
      - office.Office.Document.getTaskAsync
      - office.Office.Document.getTaskAsync_1
      - office.Office.Document.getTaskByIndexAsync
      - office.Office.Document.getTaskByIndexAsync_1
      - office.Office.Document.getTaskFieldAsync
      - office.Office.Document.getTaskFieldAsync_1
      - office.Office.Document.getWSSUrlAsync
      - office.Office.Document.getWSSUrlAsync_1
      - office.Office.Document.goToByIdAsync
      - office.Office.Document.goToByIdAsync_1
      - office.Office.Document.removeHandlerAsync
      - office.Office.Document.removeHandlerAsync_1
      - office.Office.Document.setResourceFieldAsync
      - office.Office.Document.setResourceFieldAsync_1
      - office.Office.Document.setSelectedDataAsync
      - office.Office.Document.setSelectedDataAsync_1
      - office.Office.Document.setTaskFieldAsync
      - office.Office.Document.setTaskFieldAsync_1
      - office.Office.Document.bindings
      - office.Office.Document.customXmlParts
      - office.Office.Document.mode
      - office.Office.Document.settings
      - office.Office.Document.url
  - uid: office.Office.Document.addHandlerAsync
    summary: <span data-ttu-id="e7222-103">为 Document 对象事件添加事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-104">**要求集**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="e7222-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="e7222-105">只要每个事件处理程序函数的名称是唯一的, 您就可以为指定的事件类型添加多个事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="e7222-106">对于 Document 对象事件, 可将事件的参数指定为`Office.EventType.Document.SelectionChanged`或`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="e7222-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="e7222-107">, 或此枚举的对应文本值。</span><span class="sxs-lookup"><span data-stu-id="e7222-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="e7222-108">要添加的事件处理程序函数, 其唯一的参数的类型为[DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="e7222-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="e7222-109">.</span><span class="sxs-lookup"><span data-stu-id="e7222-109"></span></span> <span data-ttu-id="e7222-110">必需。</span><span class="sxs-lookup"><span data-stu-id="e7222-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="e7222-111">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-112">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-112">Optional.</span></span> <span data-ttu-id="e7222-113">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-114">.</span><span class="sxs-lookup"><span data-stu-id="e7222-114"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.addHandlerAsync_1
    summary: <span data-ttu-id="e7222-115">为 Document 对象事件添加事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-116">**要求集**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="e7222-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="e7222-117">只要每个事件处理程序函数的名称是唯一的, 您就可以为指定的事件类型添加多个事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-118">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="e7222-119">对于 Document 对象事件, 可将事件的参数指定为`Office.EventType.Document.SelectionChanged`或`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="e7222-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="e7222-120">, 或此枚举的对应文本值。</span><span class="sxs-lookup"><span data-stu-id="e7222-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="e7222-121">要添加的事件处理程序函数, 其唯一的参数的类型为[DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="e7222-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="e7222-122">.</span><span class="sxs-lookup"><span data-stu-id="e7222-122"></span></span> <span data-ttu-id="e7222-123">必需。</span><span class="sxs-lookup"><span data-stu-id="e7222-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="e7222-124">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-124">Optional.</span></span> <span data-ttu-id="e7222-125">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-126">.</span><span class="sxs-lookup"><span data-stu-id="e7222-126"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.getActiveViewAsync
    summary: <span data-ttu-id="e7222-127">返回演示文稿（编辑或读取）的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="e7222-127">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="e7222-128">**要求集**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="e7222-128">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="e7222-129">当视图更改时可以触发事件。</span><span class="sxs-lookup"><span data-stu-id="e7222-129">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-130">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-130">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-131">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-131">Optional.</span></span> <span data-ttu-id="e7222-132">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-132">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-133">.</span><span class="sxs-lookup"><span data-stu-id="e7222-133"></span></span> <span data-ttu-id="e7222-134">结果`value`的属性是演示文稿的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="e7222-134">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="e7222-135">返回的值可以是 "编辑" 或 "读取"。</span><span class="sxs-lookup"><span data-stu-id="e7222-135">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="e7222-136">"编辑" 对应于可在其中编辑幻灯片的任何视图, 如 "普通" 或 "大纲视图"。</span><span class="sxs-lookup"><span data-stu-id="e7222-136">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="e7222-137">"读取" 对应于幻灯片放映或阅读视图。</span><span class="sxs-lookup"><span data-stu-id="e7222-137">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getActiveViewAsync_1
    summary: <span data-ttu-id="e7222-138">返回演示文稿（编辑或读取）的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="e7222-138">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="e7222-139">**要求集**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="e7222-139">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="e7222-140">当视图更改时可以触发事件。</span><span class="sxs-lookup"><span data-stu-id="e7222-140">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-141">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-141">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-142">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-142">Optional.</span></span> <span data-ttu-id="e7222-143">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-143">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-144">.</span><span class="sxs-lookup"><span data-stu-id="e7222-144"></span></span> <span data-ttu-id="e7222-145">结果`value`的属性是演示文稿的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="e7222-145">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="e7222-146">返回的值可以是 "编辑" 或 "读取"。</span><span class="sxs-lookup"><span data-stu-id="e7222-146">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="e7222-147">"编辑" 对应于可在其中编辑幻灯片的任何视图, 如 "普通" 或 "大纲视图"。</span><span class="sxs-lookup"><span data-stu-id="e7222-147">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="e7222-148">"读取" 对应于幻灯片放映或阅读视图。</span><span class="sxs-lookup"><span data-stu-id="e7222-148">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getFileAsync
    summary: '<span data-ttu-id="e7222-149">以高达 4194304 字节 (4 MB) 的切片形式返回整个文档文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-149">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="e7222-150">对于 iOS 加载项而言，支持高达 65536 (64 KB) 的文件切片。</span><span class="sxs-lookup"><span data-stu-id="e7222-150">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="e7222-151">请注意，如果指定文件切片的大小上限超出允许限制，则会导致一个“内部错误”故障。</span><span class="sxs-lookup"><span data-stu-id="e7222-151">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="e7222-152">**要求集**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (使用时`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-152">**Requirement sets**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-153">)、[文件](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span><span class="sxs-lookup"><span data-stu-id="e7222-153">), [File](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span></span><!-- --><span data-ttu-id="e7222-154">、 [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (使用`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-154">, [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="e7222-155">)</span><span class="sxs-lookup"><span data-stu-id="e7222-155"></span></span>

      <span data-ttu-id="e7222-156">对于在 office for iOS 之外的 office 主机应用程序中运行的外接程序, document.getfileasync 方法支持获取最多为4194304字节 (4 MB) 的切片中的文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-156">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="e7222-157">对于运行在 Office for iOS 应用中的加载项，getFileAsync 方法支持以切片形式获取至多 65536 (64 KB) 的文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-157">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="e7222-158">可以使用 " [Office 文件类型](xref:office.Office.FileType)" 枚举或文本值指定文件类型参数。</span><span class="sxs-lookup"><span data-stu-id="e7222-158">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="e7222-159">但可能的值因主机而异:</span><span class="sxs-lookup"><span data-stu-id="e7222-159">But the possible values vary with the host:</span></span>

      <span data-ttu-id="e7222-160">适用于 Windows desktop、iPad 和 excel Online 的 excel:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-160">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="e7222-161">Excel for Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-161">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-162">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-162"></span></span>

      <span data-ttu-id="e7222-163">powerpoint for Windows desktop、Mac、iPad 和 PowerPoint Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-163">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-164">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-164"></span></span>

      <span data-ttu-id="e7222-165">word for Windows desktop、Mac、iPad 和 Word Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-165">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-166">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-166"></span></span><!-- --><span data-ttu-id="e7222-167">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-167"></span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-168">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-168">Examples</span></span>

      ```javascript
      // The following example gets the document in Office Open XML ("compressed") format in 65536 bytes (64 KB) slices.
      // Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.
      function getDocumentAsCompressed() {
          Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, 
              function (result) {
                  if (result.status == "succeeded") {
                  // If the getFileAsync call succeeded, then
                  // result.value will return a valid File Object.
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);

                  // Get the file slices.
                  getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
                  else {
                  app.showNotification("Error:", result.error.message);
                  }
          });
      }

      function getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {
          file.getSliceAsync(nextSlice, function (sliceResult) {
              if (sliceResult.status == "succeeded") {
                  if (!gotAllSlices) { // Failed to get all slices, no need to continue.
                      return;
                  }

                  // Got one slice, store it in a temporary array.
                  // (Or you can do something else, such as
                  // send it to a third-party server.)
                  docdataSlices[sliceResult.value.index] = sliceResult.value.data;
                  if (++slicesReceived == sliceCount) {
                    // All slices have been received.
                    file.closeAsync();
                    onGotAllSlices(docdataSlices);
                  }
                  else {
                      getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
              }
                  else {
                      gotAllSlices = false;
                      file.closeAsync();
                      app.showNotification("getSliceAsync Error:", sliceResult.error.message);
                  }
          });
      }

      function onGotAllSlices(docdataSlices) {
          var docdata = [];
          for (var i = 0; i < docdataSlices.length; i++) {
              docdata = docdata.concat(docdataSlices[i]);
          }

          var fileContent = new String();
          for (var j = 0; j < docdata.length; j++) {
              fileContent += String.fromCharCode(docdata[j]);
          }

          // Now all the file content is stored in 'fileContent' variable,
          // you can do something with it, such as print, fax...
      }

      // The following example gets the document in PDF format.
      Office.context.document.getFileAsync(Office.FileType.Pdf,
          function(result) {
              if (result.status == "succeeded") {
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);
                  // Now, you can call getSliceAsync to download the files,
                  // as described in the previous code segment (compressed format).
                  
                  myFile.closeAsync();
              }
              else {
                  app.showNotification("Error:", result.error.message);
              }
      }
      );
      ```
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="e7222-169">将返回文件的格式</span><span class="sxs-lookup"><span data-stu-id="e7222-169">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: options
          description: <span data-ttu-id="e7222-170">提供用于设置要将文档拆分成的扇区大小的选项。</span><span class="sxs-lookup"><span data-stu-id="e7222-170">Provides options for setting the size of slices that the document will be divided into.</span></span>
          type:
            - office.Office.GetFileOptions
        - id: callback
          description: '<span data-ttu-id="e7222-171">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-171">Optional.</span></span> <span data-ttu-id="e7222-172">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-172">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-173">.</span><span class="sxs-lookup"><span data-stu-id="e7222-173"></span></span> <span data-ttu-id="e7222-174">结果`value`的属性是 File 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-174">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFileAsync_1
    summary: '<span data-ttu-id="e7222-175">以高达 4194304 字节 (4 MB) 的切片形式返回整个文档文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-175">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="e7222-176">对于 iOS 加载项而言，支持高达 65536 (64 KB) 的文件切片。</span><span class="sxs-lookup"><span data-stu-id="e7222-176">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="e7222-177">请注意，如果指定文件切片的大小上限超出允许限制，则会导致一个“内部错误”故障。</span><span class="sxs-lookup"><span data-stu-id="e7222-177">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="e7222-178">**要求集**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (使用时`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-178">**Requirement sets**: [CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-179">)、[文件](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span><span class="sxs-lookup"><span data-stu-id="e7222-179">), [File](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)</span></span><!-- --><span data-ttu-id="e7222-180">、 [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (使用`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-180">, [TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="e7222-181">)</span><span class="sxs-lookup"><span data-stu-id="e7222-181"></span></span>

      <span data-ttu-id="e7222-182">对于在 office for iOS 之外的 office 主机应用程序中运行的外接程序, document.getfileasync 方法支持获取最多为4194304字节 (4 MB) 的切片中的文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-182">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="e7222-183">对于运行在 Office for iOS 应用中的加载项，getFileAsync 方法支持以切片形式获取至多 65536 (64 KB) 的文件。</span><span class="sxs-lookup"><span data-stu-id="e7222-183">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="e7222-184">可以使用 " [Office 文件类型](xref:office.Office.FileType)" 枚举或文本值指定文件类型参数。</span><span class="sxs-lookup"><span data-stu-id="e7222-184">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="e7222-185">但可能的值因主机而异:</span><span class="sxs-lookup"><span data-stu-id="e7222-185">But the possible values vary with the host:</span></span>

      <span data-ttu-id="e7222-186">适用于 Windows desktop、iPad 和 excel Online 的 excel:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-186">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="e7222-187">Excel for Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-187">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-188">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-188"></span></span>

      <span data-ttu-id="e7222-189">powerpoint for Windows desktop、Mac、iPad 和 PowerPoint Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-189">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-190">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-190"></span></span>

      <span data-ttu-id="e7222-191">word for Windows desktop、Mac、iPad 和 Word Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="e7222-191">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="e7222-192">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="e7222-192"></span></span><!-- --><span data-ttu-id="e7222-193">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-193"></span></span>
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="e7222-194">将返回文件的格式</span><span class="sxs-lookup"><span data-stu-id="e7222-194">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: callback
          description: '<span data-ttu-id="e7222-195">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-195">Optional.</span></span> <span data-ttu-id="e7222-196">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-196">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-197">.</span><span class="sxs-lookup"><span data-stu-id="e7222-197"></span></span> <span data-ttu-id="e7222-198">结果`value`的属性是 File 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-198">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync
    summary: <span data-ttu-id="e7222-199">获取当前文档的文件属性。</span><span class="sxs-lookup"><span data-stu-id="e7222-199">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-200">**要求集**:[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="e7222-200">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="e7222-201">您可以使用 url 属性获取文件的 url`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="e7222-201">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="e7222-202">.</span><span class="sxs-lookup"><span data-stu-id="e7222-202"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-203">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-203">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-204">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-204">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-205">.</span><span class="sxs-lookup"><span data-stu-id="e7222-205"></span></span> <span data-ttu-id="e7222-206">结果`value`的属性是文件的属性 (URL 位于`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="e7222-206">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="e7222-207">).</span><span class="sxs-lookup"><span data-stu-id="e7222-207"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync_1
    summary: <span data-ttu-id="e7222-208">获取当前文档的文件属性。</span><span class="sxs-lookup"><span data-stu-id="e7222-208">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-209">**要求集**:[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="e7222-209">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="e7222-210">您可以使用 url 属性获取文件的 url`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="e7222-210">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="e7222-211">.</span><span class="sxs-lookup"><span data-stu-id="e7222-211"></span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-212">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-212">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-213">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-213">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-214">.</span><span class="sxs-lookup"><span data-stu-id="e7222-214"></span></span> <span data-ttu-id="e7222-215">结果`value`的属性是文件的属性 (URL 位于`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="e7222-215">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="e7222-216">).</span><span class="sxs-lookup"><span data-stu-id="e7222-216"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync
    summary: |-
      <span data-ttu-id="e7222-217">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-217">Project documents only.</span></span> <span data-ttu-id="e7222-218">获取当前项目中的资源集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="e7222-218">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="e7222-219">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-219">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-220">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-220">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-221">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-221">Optional.</span></span> <span data-ttu-id="e7222-222">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-222">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-223">.</span><span class="sxs-lookup"><span data-stu-id="e7222-223"></span></span> <span data-ttu-id="e7222-224">结果`value`的属性是当前项目的资源集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="e7222-224">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync_1
    summary: |-
      <span data-ttu-id="e7222-225">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-225">Project documents only.</span></span> <span data-ttu-id="e7222-226">获取当前项目中的资源集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="e7222-226">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="e7222-227">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-227">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-228">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-228">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-229">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-229">Optional.</span></span> <span data-ttu-id="e7222-230">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-230">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-231">.</span><span class="sxs-lookup"><span data-stu-id="e7222-231"></span></span> <span data-ttu-id="e7222-232">结果`value`的属性是当前项目的资源集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="e7222-232">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync
    summary: |-
      <span data-ttu-id="e7222-233">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-233">Project documents only.</span></span> <span data-ttu-id="e7222-234">获取当前项目中的任务集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="e7222-234">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="e7222-235">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-235">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-236">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-236">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-237">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-237">Optional.</span></span> <span data-ttu-id="e7222-238">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-238">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-239">.</span><span class="sxs-lookup"><span data-stu-id="e7222-239"></span></span> <span data-ttu-id="e7222-240">结果`value`的属性是当前项目的任务集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="e7222-240">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync_1
    summary: |-
      <span data-ttu-id="e7222-241">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-241">Project documents only.</span></span> <span data-ttu-id="e7222-242">获取当前项目中的任务集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="e7222-242">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="e7222-243">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-243">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-244">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-244">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-245">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-245">Optional.</span></span> <span data-ttu-id="e7222-246">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-246">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-247">.</span><span class="sxs-lookup"><span data-stu-id="e7222-247"></span></span> <span data-ttu-id="e7222-248">结果`value`的属性是当前项目的任务集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="e7222-248">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getProjectFieldAsync
    summary: <span data-ttu-id="e7222-249">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-249">Project documents only.</span></span> <span data-ttu-id="e7222-250">获取项目域 (例如</span><span class="sxs-lookup"><span data-stu-id="e7222-250">Get Project field (Ex.</span></span> <span data-ttu-id="e7222-251">ProjectWebAccessURL)。</span><span class="sxs-lookup"><span data-stu-id="e7222-251">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="e7222-252">项目级别字段。</span><span class="sxs-lookup"><span data-stu-id="e7222-252">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="e7222-253">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-253">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-254">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-254">Optional.</span></span> <span data-ttu-id="e7222-255">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-255">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-256">.</span><span class="sxs-lookup"><span data-stu-id="e7222-256"></span></span> <span data-ttu-id="e7222-257">结果`value`的属性包含`fieldValue`属性, 它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-257">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getProjectFieldAsync_1
    summary: <span data-ttu-id="e7222-258">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-258">Project documents only.</span></span> <span data-ttu-id="e7222-259">获取项目域 (例如</span><span class="sxs-lookup"><span data-stu-id="e7222-259">Get Project field (Ex.</span></span> <span data-ttu-id="e7222-260">ProjectWebAccessURL)。</span><span class="sxs-lookup"><span data-stu-id="e7222-260">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-261">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-261">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="e7222-262">项目级别字段。</span><span class="sxs-lookup"><span data-stu-id="e7222-262">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="e7222-263">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-263">Optional.</span></span> <span data-ttu-id="e7222-264">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-264">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-265">.</span><span class="sxs-lookup"><span data-stu-id="e7222-265"></span></span> <span data-ttu-id="e7222-266">结果`value`的属性包含`fieldValue`属性, 它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-266">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync
    summary: |-
      <span data-ttu-id="e7222-267">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-267">Project documents only.</span></span> <span data-ttu-id="e7222-268">获取资源集合中具有指定索引的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-268">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="e7222-269">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-269">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="e7222-270">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="e7222-270">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="e7222-271">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-271">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-272">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-272">Optional.</span></span> <span data-ttu-id="e7222-273">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-273">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-274">.</span><span class="sxs-lookup"><span data-stu-id="e7222-274"></span></span> <span data-ttu-id="e7222-275">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-275">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync_1
    summary: |-
      <span data-ttu-id="e7222-276">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-276">Project documents only.</span></span> <span data-ttu-id="e7222-277">获取资源集合中具有指定索引的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-277">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="e7222-278">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-278">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-279">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-279">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="e7222-280">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="e7222-280">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="e7222-281">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-281">Optional.</span></span> <span data-ttu-id="e7222-282">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-282">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-283">.</span><span class="sxs-lookup"><span data-stu-id="e7222-283"></span></span> <span data-ttu-id="e7222-284">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-284">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync
    summary: <span data-ttu-id="e7222-285">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-285">Project documents only.</span></span> <span data-ttu-id="e7222-286">获取所提供资源 Id 的资源域。 (Ex)</span><span class="sxs-lookup"><span data-stu-id="e7222-286">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="e7222-287">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-287">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-288">资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-288">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="e7222-289">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-289">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-290">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-290">Optional.</span></span> <span data-ttu-id="e7222-291">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-291">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-292">.</span><span class="sxs-lookup"><span data-stu-id="e7222-292"></span></span> <span data-ttu-id="e7222-293">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-293">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync_1
    summary: <span data-ttu-id="e7222-294">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-294">Project documents only.</span></span> <span data-ttu-id="e7222-295">获取所提供资源 Id 的资源域。 (Ex)</span><span class="sxs-lookup"><span data-stu-id="e7222-295">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-296">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-296">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="e7222-297">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-297">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-298">资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-298">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="e7222-299">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-299">Optional.</span></span> <span data-ttu-id="e7222-300">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-300">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-301">.</span><span class="sxs-lookup"><span data-stu-id="e7222-301"></span></span> <span data-ttu-id="e7222-302">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-302">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedDataAsync
    summary: <span data-ttu-id="e7222-303">读取包含在文档的当前选择中的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-303">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-304">**要求集**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="e7222-304">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="e7222-305">)、 [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (使用`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="e7222-305">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="e7222-306">)、 [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (使用`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="e7222-306">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="e7222-307">)、[选择](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="e7222-307">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="e7222-308">、 [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (使用`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="e7222-308">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="e7222-309">)、 [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (使用`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-309">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="e7222-310">)</span><span class="sxs-lookup"><span data-stu-id="e7222-310"></span></span>

      <span data-ttu-id="e7222-311">在传递给 getSelectedDataAsync 方法的回调函数中, 您可以使用 AsyncResult 对象的属性返回以下信息。</span><span class="sxs-lookup"><span data-stu-id="e7222-311">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-312">属性</span><span class="sxs-lookup"><span data-stu-id="e7222-312">Property</span></span></th> <th><span data-ttu-id="e7222-313">用于...</span><span class="sxs-lookup"><span data-stu-id="e7222-313">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-314">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="e7222-314">AsyncResult.value</span></span></td> <td><span data-ttu-id="e7222-315">始终返回 undefined，这是因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-315">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-316">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="e7222-316">AsyncResult.status</span></span></td> <td><span data-ttu-id="e7222-317">确定操作是成功还是失败。</span><span class="sxs-lookup"><span data-stu-id="e7222-317">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-318">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="e7222-318">AsyncResult.error</span></span></td> <td><span data-ttu-id="e7222-319">如果操作失败，则访问提供错误信息的 Error 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-319">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-320">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="e7222-320">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="e7222-321">在 AsyncResult 对象中未经改动的返回的任何类型的用户定义项。</span><span class="sxs-lookup"><span data-stu-id="e7222-321">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="e7222-322">[CoercionType](xref:office.Office.CoercionType)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="e7222-322">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-323">主机</span><span class="sxs-lookup"><span data-stu-id="e7222-323">Host</span></span></th> <th><span data-ttu-id="e7222-324">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="e7222-324">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-325">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-325">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="e7222-326">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="e7222-326">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-327">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-327">Excel and Word</span></span></td> <td><span data-ttu-id="e7222-328">`Office.CoercionType.Matrix`(数组的数组)</span><span class="sxs-lookup"><span data-stu-id="e7222-328">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-329">Access、Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-329">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="e7222-330">`Office.CoercionType.Table`(TableData 对象)</span><span class="sxs-lookup"><span data-stu-id="e7222-330">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-331">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-331">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="e7222-332">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-332">Word</span></span></td> <td><span data-ttu-id="e7222-333">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="e7222-333">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-334">PowerPoint 和 PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="e7222-334">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="e7222-335">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-335">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="e7222-336">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-336">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: '<span data-ttu-id="e7222-337">要返回的数据结构的类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-337">The type of data structure to return.</span></span> <span data-ttu-id="e7222-338">有关每个主机支持的强制类型, 请参阅 "备注" 部分。</span><span class="sxs-lookup"><span data-stu-id="e7222-338">See the remarks section for each host''s supported coercion types.</span></span>'
          type:
            - office.Office.CoercionType
        - id: options
          description: <span data-ttu-id="e7222-339">提供用于自定义要返回的数据及其格式的选项。</span><span class="sxs-lookup"><span data-stu-id="e7222-339">Provides options for customizing what data is returned and how it is formatted.</span></span>
          type:
            - office.Office.GetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="e7222-340">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-340">Optional.</span></span> <span data-ttu-id="e7222-341">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-341">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-342">.</span><span class="sxs-lookup"><span data-stu-id="e7222-342"></span></span> <span data-ttu-id="e7222-343">结果`value`的属性为当前所选内容中的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-343">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="e7222-344">这是使用 coercionType 参数指定的数据结构或格式返回的。</span><span class="sxs-lookup"><span data-stu-id="e7222-344">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="e7222-345">（请参阅备注，了解有关数据强制的详细信息。）</span><span class="sxs-lookup"><span data-stu-id="e7222-345">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedDataAsync_1
    summary: <span data-ttu-id="e7222-346">读取包含在文档的当前选择中的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-346">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-347">**要求集**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="e7222-347">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="e7222-348">)、 [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (使用`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="e7222-348">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="e7222-349">)、 [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (使用`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="e7222-349">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="e7222-350">)、[选择](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="e7222-350">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="e7222-351">、 [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (使用`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="e7222-351">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="e7222-352">)、 [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (使用`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-352">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="e7222-353">)</span><span class="sxs-lookup"><span data-stu-id="e7222-353"></span></span>

      <span data-ttu-id="e7222-354">在传递给 getSelectedDataAsync 方法的回调函数中, 您可以使用 AsyncResult 对象的属性返回以下信息。</span><span class="sxs-lookup"><span data-stu-id="e7222-354">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-355">属性</span><span class="sxs-lookup"><span data-stu-id="e7222-355">Property</span></span></th> <th><span data-ttu-id="e7222-356">用于...</span><span class="sxs-lookup"><span data-stu-id="e7222-356">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-357">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="e7222-357">AsyncResult.value</span></span></td> <td><span data-ttu-id="e7222-358">始终返回 undefined，这是因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-358">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-359">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="e7222-359">AsyncResult.status</span></span></td> <td><span data-ttu-id="e7222-360">确定操作是成功还是失败。</span><span class="sxs-lookup"><span data-stu-id="e7222-360">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-361">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="e7222-361">AsyncResult.error</span></span></td> <td><span data-ttu-id="e7222-362">如果操作失败，则访问提供错误信息的 Error 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-362">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-363">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="e7222-363">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="e7222-364">在 AsyncResult 对象中未经改动的返回的任何类型的用户定义项。</span><span class="sxs-lookup"><span data-stu-id="e7222-364">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="e7222-365">[CoercionType](xref:office.Office.CoercionType)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="e7222-365">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-366">主机</span><span class="sxs-lookup"><span data-stu-id="e7222-366">Host</span></span></th> <th><span data-ttu-id="e7222-367">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="e7222-367">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-368">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-368">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="e7222-369">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="e7222-369">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-370">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-370">Excel and Word</span></span></td> <td><span data-ttu-id="e7222-371">`Office.CoercionType.Matrix`(数组的数组)</span><span class="sxs-lookup"><span data-stu-id="e7222-371">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-372">Access、Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-372">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="e7222-373">`Office.CoercionType.Table`(TableData 对象)</span><span class="sxs-lookup"><span data-stu-id="e7222-373">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-374">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-374">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="e7222-375">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-375">Word</span></span></td> <td><span data-ttu-id="e7222-376">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="e7222-376">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-377">PowerPoint 和 PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="e7222-377">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="e7222-378">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-378">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: '<span data-ttu-id="e7222-379">要返回的数据结构的类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-379">The type of data structure to return.</span></span> <span data-ttu-id="e7222-380">有关每个主机支持的强制类型, 请参阅 "备注" 部分。</span><span class="sxs-lookup"><span data-stu-id="e7222-380">See the remarks section for each host''s supported coercion types.</span></span>'
          type:
            - office.Office.CoercionType
        - id: callback
          description: '<span data-ttu-id="e7222-381">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-381">Optional.</span></span> <span data-ttu-id="e7222-382">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-382">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-383">.</span><span class="sxs-lookup"><span data-stu-id="e7222-383"></span></span> <span data-ttu-id="e7222-384">结果`value`的属性为当前所选内容中的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-384">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="e7222-385">这是使用 coercionType 参数指定的数据结构或格式返回的。</span><span class="sxs-lookup"><span data-stu-id="e7222-385">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="e7222-386">（请参阅备注，了解有关数据强制的详细信息。）</span><span class="sxs-lookup"><span data-stu-id="e7222-386">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync
    summary: <span data-ttu-id="e7222-387">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-387">Project documents only.</span></span> <span data-ttu-id="e7222-388">获取当前选定的资源的 Id。</span><span class="sxs-lookup"><span data-stu-id="e7222-388">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-389">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-389">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-390">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-390">Optional.</span></span> <span data-ttu-id="e7222-391">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-391">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-392">.</span><span class="sxs-lookup"><span data-stu-id="e7222-392"></span></span> <span data-ttu-id="e7222-393">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-393">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync_1
    summary: <span data-ttu-id="e7222-394">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-394">Project documents only.</span></span> <span data-ttu-id="e7222-395">获取当前选定的资源的 Id。</span><span class="sxs-lookup"><span data-stu-id="e7222-395">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-396">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-396">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-397">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-397">Optional.</span></span> <span data-ttu-id="e7222-398">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-398">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-399">.</span><span class="sxs-lookup"><span data-stu-id="e7222-399"></span></span> <span data-ttu-id="e7222-400">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-400">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync
    summary: <span data-ttu-id="e7222-401">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-401">Project documents only.</span></span> <span data-ttu-id="e7222-402">获取当前所选任务的 Id。</span><span class="sxs-lookup"><span data-stu-id="e7222-402">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-403">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-403">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-404">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-404">Optional.</span></span> <span data-ttu-id="e7222-405">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-405">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-406">.</span><span class="sxs-lookup"><span data-stu-id="e7222-406"></span></span> <span data-ttu-id="e7222-407">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-407">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync_1
    summary: <span data-ttu-id="e7222-408">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-408">Project documents only.</span></span> <span data-ttu-id="e7222-409">获取当前所选任务的 Id。</span><span class="sxs-lookup"><span data-stu-id="e7222-409">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-410">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-410">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-411">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-411">Optional.</span></span> <span data-ttu-id="e7222-412">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-412">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-413">.</span><span class="sxs-lookup"><span data-stu-id="e7222-413"></span></span> <span data-ttu-id="e7222-414">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-414">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedViewAsync
    summary: <span data-ttu-id="e7222-415">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-415">Project documents only.</span></span> <span data-ttu-id="e7222-416">获取当前选定的视图类型 (Ex)。</span><span class="sxs-lookup"><span data-stu-id="e7222-416">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="e7222-417">甘特图) 和视图名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-417">Gantt) and View Name.</span></span>
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-418">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-418">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-419">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-419">Optional.</span></span> <span data-ttu-id="e7222-420">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-420">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-421">.</span><span class="sxs-lookup"><span data-stu-id="e7222-421"></span></span> <span data-ttu-id="e7222-422">结果`value`的属性包含以下属性: `viewName`视图的名称, 作为 ProjectViewTypes 常量。</span><span class="sxs-lookup"><span data-stu-id="e7222-422">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="e7222-423">`viewType`-视图的类型, 作为 ProjectViewTypes 常量的整数值。</span><span class="sxs-lookup"><span data-stu-id="e7222-423">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getSelectedViewAsync_1
    summary: <span data-ttu-id="e7222-424">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-424">Project documents only.</span></span> <span data-ttu-id="e7222-425">获取当前选定的视图类型 (Ex)。</span><span class="sxs-lookup"><span data-stu-id="e7222-425">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="e7222-426">甘特图) 和视图名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-426">Gantt) and View Name.</span></span>
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-427">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-427">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-428">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-428">Optional.</span></span> <span data-ttu-id="e7222-429">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-429">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-430">.</span><span class="sxs-lookup"><span data-stu-id="e7222-430"></span></span> <span data-ttu-id="e7222-431">结果`value`的属性包含以下属性: `viewName`视图的名称, 作为 ProjectViewTypes 常量。</span><span class="sxs-lookup"><span data-stu-id="e7222-431">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="e7222-432">`viewType`-视图的类型, 作为 ProjectViewTypes 常量的整数值。</span><span class="sxs-lookup"><span data-stu-id="e7222-432">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync
    summary: '<span data-ttu-id="e7222-433">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-433">Project documents only.</span></span> <span data-ttu-id="e7222-434">获取给定 taskId 的任务名称、WSS 任务 Id 和 ResourceNames。</span><span class="sxs-lookup"><span data-stu-id="e7222-434">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-435">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-435">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="e7222-436">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-436">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-437">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-437">Optional.</span></span> <span data-ttu-id="e7222-438">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-438">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-439">.</span><span class="sxs-lookup"><span data-stu-id="e7222-439"></span></span> <span data-ttu-id="e7222-440">结果`value`的属性包含以下属性: `taskName` -任务的名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-440">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="e7222-441">`wssTaskId`-同步的 SharePoint 任务列表中任务的 ID。</span><span class="sxs-lookup"><span data-stu-id="e7222-441">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="e7222-442">如果项目未与 SharePoint 任务列表同步，则值为 0。</span><span class="sxs-lookup"><span data-stu-id="e7222-442">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="e7222-443">`resourceNames`-分配给任务的资源的名称的逗号分隔列表。</span><span class="sxs-lookup"><span data-stu-id="e7222-443">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync_1
    summary: '<span data-ttu-id="e7222-444">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-444">Project documents only.</span></span> <span data-ttu-id="e7222-445">获取给定 taskId 的任务名称、WSS 任务 Id 和 ResourceNames。</span><span class="sxs-lookup"><span data-stu-id="e7222-445">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-446">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-446">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-447">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-447">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="e7222-448">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-448">Optional.</span></span> <span data-ttu-id="e7222-449">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-449">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-450">.</span><span class="sxs-lookup"><span data-stu-id="e7222-450"></span></span> <span data-ttu-id="e7222-451">结果`value`的属性包含以下属性: `taskName` -任务的名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-451">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="e7222-452">`wssTaskId`-同步的 SharePoint 任务列表中任务的 ID。</span><span class="sxs-lookup"><span data-stu-id="e7222-452">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="e7222-453">如果项目未与 SharePoint 任务列表同步，则值为 0。</span><span class="sxs-lookup"><span data-stu-id="e7222-453">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="e7222-454">`resourceNames`-分配给任务的资源的名称的逗号分隔列表。</span><span class="sxs-lookup"><span data-stu-id="e7222-454">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync
    summary: |-
      <span data-ttu-id="e7222-455">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-455">Project documents only.</span></span> <span data-ttu-id="e7222-456">获取任务集合中具有指定索引的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-456">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="e7222-457">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-457">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="e7222-458">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="e7222-458">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="e7222-459">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-459">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-460">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-460">Optional.</span></span> <span data-ttu-id="e7222-461">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-461">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-462">.</span><span class="sxs-lookup"><span data-stu-id="e7222-462"></span></span> <span data-ttu-id="e7222-463">结果`value`的属性是作为字符串的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-463">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync_1
    summary: |-
      <span data-ttu-id="e7222-464">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-464">Project documents only.</span></span> <span data-ttu-id="e7222-465">获取任务集合中具有指定索引的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-465">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="e7222-466">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-466">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-467">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-467">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="e7222-468">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="e7222-468">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="e7222-469">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-469">Optional.</span></span> <span data-ttu-id="e7222-470">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-470">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-471">.</span><span class="sxs-lookup"><span data-stu-id="e7222-471"></span></span> <span data-ttu-id="e7222-472">结果`value`的属性是作为字符串的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="e7222-472">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskFieldAsync
    summary: '<span data-ttu-id="e7222-473">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-473">Project documents only.</span></span> <span data-ttu-id="e7222-474">获取所提供任务 Id 的任务域。 (例如,</span><span class="sxs-lookup"><span data-stu-id="e7222-474">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="e7222-475">起始日期)。</span><span class="sxs-lookup"><span data-stu-id="e7222-475">StartDate).</span></span>'
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-476">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-476">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-477">任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-477">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="e7222-478">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-478">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-479">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-479">Optional.</span></span> <span data-ttu-id="e7222-480">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-480">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-481">.</span><span class="sxs-lookup"><span data-stu-id="e7222-481"></span></span> <span data-ttu-id="e7222-482">结果`value`的属性包含`fieldValue`属性, 它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-482">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskFieldAsync_1
    summary: '<span data-ttu-id="e7222-483">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-483">Project documents only.</span></span> <span data-ttu-id="e7222-484">获取所提供任务 Id 的任务域。 (例如,</span><span class="sxs-lookup"><span data-stu-id="e7222-484">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="e7222-485">起始日期)。</span><span class="sxs-lookup"><span data-stu-id="e7222-485">StartDate).</span></span>'
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-486">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-486">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-487">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-487">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-488">任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-488">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="e7222-489">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-489">Optional.</span></span> <span data-ttu-id="e7222-490">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-490">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-491">.</span><span class="sxs-lookup"><span data-stu-id="e7222-491"></span></span> <span data-ttu-id="e7222-492">结果`value`的属性包含`fieldValue`属性, 它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-492">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync
    summary: '<span data-ttu-id="e7222-493">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-493">Project documents only.</span></span> <span data-ttu-id="e7222-494">获取任务列表的 WSS Url 和列表名称时, MPP 也将同步。</span><span class="sxs-lookup"><span data-stu-id="e7222-494">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="e7222-495">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-495">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-496">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-496">Optional.</span></span> <span data-ttu-id="e7222-497">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-497">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-498">.</span><span class="sxs-lookup"><span data-stu-id="e7222-498"></span></span> <span data-ttu-id="e7222-499">结果`value`的属性包含以下属性: `listName` -同步的 SharePoint 任务列表的名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-499">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="e7222-500">`serverUrl`-同步的 SharePoint 任务列表的 URL。</span><span class="sxs-lookup"><span data-stu-id="e7222-500">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync_1
    summary: '<span data-ttu-id="e7222-501">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-501">Project documents only.</span></span> <span data-ttu-id="e7222-502">获取任务列表的 WSS Url 和列表名称时, MPP 也将同步。</span><span class="sxs-lookup"><span data-stu-id="e7222-502">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="e7222-503">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-503">Optional.</span></span> <span data-ttu-id="e7222-504">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-504">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-505">.</span><span class="sxs-lookup"><span data-stu-id="e7222-505"></span></span> <span data-ttu-id="e7222-506">结果`value`的属性包含以下属性: `listName` -同步的 SharePoint 任务列表的名称。</span><span class="sxs-lookup"><span data-stu-id="e7222-506">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="e7222-507">`serverUrl`-同步的 SharePoint 任务列表的 URL。</span><span class="sxs-lookup"><span data-stu-id="e7222-507">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync
    summary: <span data-ttu-id="e7222-508">转到文档中指定的对象或位置。</span><span class="sxs-lookup"><span data-stu-id="e7222-508">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-509">**要求集**:[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="e7222-509">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="e7222-510">PowerPoint 不支持母版视图中的 document.gotobyidasync 方法。</span><span class="sxs-lookup"><span data-stu-id="e7222-510">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="e7222-511">selectionMode 选项导致的行为因主机而异:</span><span class="sxs-lookup"><span data-stu-id="e7222-511">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="e7222-512">在 Excel 中`Office.SelectionMode.Selected` : 选择绑定或已命名项目中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-512">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="e7222-513">Office.SelectionMode.None 对于文本绑定，选择单元格；对于矩阵绑定、表绑定和已命名的项目，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="e7222-513">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="e7222-514">在 PowerPoint 中`Office.SelectionMode.Selected` : 选择幻灯片标题或幻灯片上的第一个文本框。</span><span class="sxs-lookup"><span data-stu-id="e7222-514">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="e7222-515">`Office.SelectionMode.None`不会选择任何内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-515">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="e7222-516">在 Word 中`Office.SelectionMode.Selected` : 选择绑定中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-516">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="e7222-517">Office.SelectionMode.None 对于文本绑定，将光标移到文本开头；对于矩阵绑定和表绑定，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="e7222-517">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="e7222-518">要转到的对象或位置的标识符。</span><span class="sxs-lookup"><span data-stu-id="e7222-518">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="e7222-519">要转到的位置类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-519">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: options
          description: <span data-ttu-id="e7222-520">提供用于决定是否要选择导航到的位置的选项。</span><span class="sxs-lookup"><span data-stu-id="e7222-520">Provides options for whether to select the location that is navigated to.</span></span>
          type:
            - office.Office.GoToByIdOptions
        - id: callback
          description: '<span data-ttu-id="e7222-521">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-521">Optional.</span></span> <span data-ttu-id="e7222-522">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-522">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-523">.</span><span class="sxs-lookup"><span data-stu-id="e7222-523"></span></span> <span data-ttu-id="e7222-524">结果`value`的属性是当前视图。</span><span class="sxs-lookup"><span data-stu-id="e7222-524">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync_1
    summary: <span data-ttu-id="e7222-525">转到文档中指定的对象或位置。</span><span class="sxs-lookup"><span data-stu-id="e7222-525">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-526">**要求集**:[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="e7222-526">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="e7222-527">PowerPoint 不支持母版视图中的 document.gotobyidasync 方法。</span><span class="sxs-lookup"><span data-stu-id="e7222-527">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="e7222-528">selectionMode 选项导致的行为因主机而异:</span><span class="sxs-lookup"><span data-stu-id="e7222-528">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="e7222-529">在 Excel 中`Office.SelectionMode.Selected` : 选择绑定或已命名项目中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-529">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="e7222-530">Office.SelectionMode.None 对于文本绑定，选择单元格；对于矩阵绑定、表绑定和已命名的项目，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="e7222-530">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="e7222-531">在 PowerPoint 中`Office.SelectionMode.Selected` : 选择幻灯片标题或幻灯片上的第一个文本框。</span><span class="sxs-lookup"><span data-stu-id="e7222-531">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="e7222-532">`Office.SelectionMode.None`不会选择任何内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-532">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="e7222-533">在 Word 中`Office.SelectionMode.Selected` : 选择绑定中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-533">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="e7222-534">Office.SelectionMode.None 对于文本绑定，将光标移到文本开头；对于矩阵绑定和表绑定，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="e7222-534">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-535">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-535">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="e7222-536">要转到的对象或位置的标识符。</span><span class="sxs-lookup"><span data-stu-id="e7222-536">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="e7222-537">要转到的位置类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-537">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: callback
          description: '<span data-ttu-id="e7222-538">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-538">Optional.</span></span> <span data-ttu-id="e7222-539">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-539">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-540">.</span><span class="sxs-lookup"><span data-stu-id="e7222-540"></span></span> <span data-ttu-id="e7222-541">结果`value`的属性是当前视图。</span><span class="sxs-lookup"><span data-stu-id="e7222-541">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.removeHandlerAsync
    summary: <span data-ttu-id="e7222-542">删除指定事件类型的事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-542">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="e7222-543">**要求集**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="e7222-543">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="e7222-544">事件类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-544">The event type.</span></span> <span data-ttu-id="e7222-545">For document 可以是 "SelectionChanged" 或 "document.activeviewchanged"。</span><span class="sxs-lookup"><span data-stu-id="e7222-545">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: options
          description: <span data-ttu-id="e7222-546">提供用于确定要删除的事件处理程序或处理程序的选项。</span><span class="sxs-lookup"><span data-stu-id="e7222-546">Provides options to determine which event handler or handlers are removed.</span></span>
          type:
            - office.Office.RemoveHandlerOptions
        - id: callback
          description: '<span data-ttu-id="e7222-547">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-547">Optional.</span></span> <span data-ttu-id="e7222-548">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-548">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-549">.</span><span class="sxs-lookup"><span data-stu-id="e7222-549"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.removeHandlerAsync_1
    summary: <span data-ttu-id="e7222-550">删除指定事件类型的事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="e7222-550">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-551">**要求集**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="e7222-551">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-552">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-552">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="e7222-553">事件类型。</span><span class="sxs-lookup"><span data-stu-id="e7222-553">The event type.</span></span> <span data-ttu-id="e7222-554">For document 可以是 "SelectionChanged" 或 "document.activeviewchanged"。</span><span class="sxs-lookup"><span data-stu-id="e7222-554">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: callback
          description: '<span data-ttu-id="e7222-555">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-555">Optional.</span></span> <span data-ttu-id="e7222-556">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-556">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-557">.</span><span class="sxs-lookup"><span data-stu-id="e7222-557"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync
    summary: |-
      <span data-ttu-id="e7222-558">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-558">Project documents only.</span></span> <span data-ttu-id="e7222-559">设置指定资源 Id 的资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-559">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="e7222-560">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-560">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="e7222-561">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-561">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-562">资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-562">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="e7222-563">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-563">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="e7222-564">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-564">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-565">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-565">Optional.</span></span> <span data-ttu-id="e7222-566">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-566">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-567">.</span><span class="sxs-lookup"><span data-stu-id="e7222-567"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync_1
    summary: |-
      <span data-ttu-id="e7222-568">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-568">Project documents only.</span></span> <span data-ttu-id="e7222-569">设置指定资源 Id 的资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-569">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="e7222-570">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-570">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-571">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-571">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="e7222-572">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-572">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-573">资源域。</span><span class="sxs-lookup"><span data-stu-id="e7222-573">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="e7222-574">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-574">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="e7222-575">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-575">Optional.</span></span> <span data-ttu-id="e7222-576">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-576">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-577">.</span><span class="sxs-lookup"><span data-stu-id="e7222-577"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync
    summary: <span data-ttu-id="e7222-578">将指定的数据写入当前所选内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-578">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-579">**要求集**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span><span class="sxs-lookup"><span data-stu-id="e7222-579">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span></span><!-- --><span data-ttu-id="e7222-580">(使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="e7222-580">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="e7222-581">)、 [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (使用`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="e7222-581">), [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="e7222-582">)、 [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (使用`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="e7222-582">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="e7222-583">)、 [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (使用`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="e7222-583">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="e7222-584">)、[选择](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="e7222-584">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="e7222-585">、 [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (使用`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="e7222-585">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="e7222-586">)、 [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (使用`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-586">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="e7222-587">)</span><span class="sxs-lookup"><span data-stu-id="e7222-587"></span></span>

      <span data-ttu-id="e7222-588">**特定于应用程序的行为**</span><span class="sxs-lookup"><span data-stu-id="e7222-588">**Application-specific behaviors**</span></span>

      <span data-ttu-id="e7222-589">以下特定于应用程序的操作在向选定内容中写入数据时适用。</span><span class="sxs-lookup"><span data-stu-id="e7222-589">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="e7222-590">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-590">Word</span></span></td> <td><span data-ttu-id="e7222-591">如果没有选定内容且插入点位于有效位置, 则在插入点插入指定`data`的</span><span class="sxs-lookup"><span data-stu-id="e7222-591">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="e7222-592">如果`data`是字符串, 则插入指定的文本。</span><span class="sxs-lookup"><span data-stu-id="e7222-592">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-593">如果`data`是数组的数组 ("矩阵") 或 TableData 对象, 则插入一个新的 Word 表。</span><span class="sxs-lookup"><span data-stu-id="e7222-593">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-594">如果`data`为 HTML, 则插入指定的 html。</span><span class="sxs-lookup"><span data-stu-id="e7222-594">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="e7222-595">(**重要说明**: 如果插入的 HTML 中的任何 HTML 无效, 则 Word 不会引发错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-595">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="e7222-596">Word 将尽可能多地插入 HTML, 并忽略任何无效数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-596">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-597">如果`data`是 Office Open xml, 则插入指定的 xml。</span><span class="sxs-lookup"><span data-stu-id="e7222-597">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-598">如果`data`是 base64 编码的图像流, 则插入指定的图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-598">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-599">如果有选定内容</span><span class="sxs-lookup"><span data-stu-id="e7222-599">If there is a selection</span></span></td> <td><span data-ttu-id="e7222-600">它将被替换为与上述`data`相同规则相同的指定项。</span><span class="sxs-lookup"><span data-stu-id="e7222-600">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-601">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-601">Insert images</span></span></td> <td><span data-ttu-id="e7222-602">插入的图像以内嵌方式放置。</span><span class="sxs-lookup"><span data-stu-id="e7222-602">Inserted images are placed inline.</span></span> <span data-ttu-id="e7222-603">imageLeft 和 imageTop 参数将被忽略。</span><span class="sxs-lookup"><span data-stu-id="e7222-603">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="e7222-604">图像的纵横比始终被锁定。</span><span class="sxs-lookup"><span data-stu-id="e7222-604">The image aspect ratio is always locked.</span></span> <span data-ttu-id="e7222-605">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-605">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-606">Excel</span><span class="sxs-lookup"><span data-stu-id="e7222-606">Excel</span></span></td> <td><span data-ttu-id="e7222-607">如果选择了单个单元格</span><span class="sxs-lookup"><span data-stu-id="e7222-607">If a single cell is selected</span></span></td> <td><span data-ttu-id="e7222-608">如果`data`是字符串, 则将指定的文本作为当前单元格的值插入。</span><span class="sxs-lookup"><span data-stu-id="e7222-608">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-609">如果`data`是数组的数组 ("矩阵"), 则插入一组指定的行和列 (如果不会覆盖周围单元格中的其他任何数据)。</span><span class="sxs-lookup"><span data-stu-id="e7222-609">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-610">如果`data`是 TableData 对象, 则插入具有一组指定的行和标题的新 Excel 表 (如果不会覆盖周围单元格中的其他任何数据)。</span><span class="sxs-lookup"><span data-stu-id="e7222-610">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-611">如果选择了多个单元格</span><span class="sxs-lookup"><span data-stu-id="e7222-611">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="e7222-612">, 则会返回错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-612">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="e7222-613">, 则根据中的值更新所选单元格的值`data`</span><span class="sxs-lookup"><span data-stu-id="e7222-613">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="e7222-614">.</span><span class="sxs-lookup"><span data-stu-id="e7222-614"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-615">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-615">Insert images</span></span></td> <td><span data-ttu-id="e7222-616">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-616">Inserted images are floating.</span></span> <span data-ttu-id="e7222-617">imageLeft 和 imageTop 位置参数是相对于当前选定的单元格而言的。</span><span class="sxs-lookup"><span data-stu-id="e7222-617">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="e7222-618">允许 imageLeft 和 imageTop 的值为负数，将由 Excel 重新调整在工作表内放置的图像位置。</span><span class="sxs-lookup"><span data-stu-id="e7222-618">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="e7222-619">除非 imageWidth 和 imageHeight 参数均已提供，否则将锁定图像的纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-619">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="e7222-620">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-620">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-621">其他所有情况</span><span class="sxs-lookup"><span data-stu-id="e7222-621">All other cases</span></span></td> <td><span data-ttu-id="e7222-622">返回错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-622">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-623">Excel Online</span><span class="sxs-lookup"><span data-stu-id="e7222-623">Excel Online</span></span></td> <td><span data-ttu-id="e7222-624">除了上述 excel 中所述的行为之外, 在 excel Online 中写入数据时也适用这些限制</span><span class="sxs-lookup"><span data-stu-id="e7222-624">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="e7222-625">对此方法的单个调用中, 可以使用`data`参数写入工作表的单元格总数不能超过20000。</span><span class="sxs-lookup"><span data-stu-id="e7222-625">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-626">传递给`cellFormat`参数的格式组的数量不能超过100。</span><span class="sxs-lookup"><span data-stu-id="e7222-626">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="e7222-627">每个格式设置组由应用于特定单元格范围的一组格式组成。</span><span class="sxs-lookup"><span data-stu-id="e7222-627">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-628">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="e7222-628">PowerPoint</span></span></td> <td><span data-ttu-id="e7222-629">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-629">Insert image</span></span></td> <td><span data-ttu-id="e7222-630">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-630">Inserted images are floating.</span></span> <span data-ttu-id="e7222-631">position imageLeft 和 imageTop 参数是可选的, 但如果提供, 则应同时存在。</span><span class="sxs-lookup"><span data-stu-id="e7222-631">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="e7222-632">如果提供单个值，则它将被忽略。</span><span class="sxs-lookup"><span data-stu-id="e7222-632">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="e7222-633">允许 imageLeft 和 imageTop 的值为负数，且可以将图像放置在幻灯片的外部。</span><span class="sxs-lookup"><span data-stu-id="e7222-633">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="e7222-634">如果未给定任何可选参数，并且幻灯片有一个占位符，则图像将替换幻灯片中的占位符。</span><span class="sxs-lookup"><span data-stu-id="e7222-634">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="e7222-635">除非 imageWidth和 imageHeight 参数均已提供，否则将锁定图像纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-635">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="e7222-636">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-636">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="e7222-637">[CoercionType](xref:office.Office.CoercionType)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="e7222-637">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-638">主机</span><span class="sxs-lookup"><span data-stu-id="e7222-638">Host</span></span></th> <th><span data-ttu-id="e7222-639">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="e7222-639">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-640">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-640">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="e7222-641">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="e7222-641">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-642">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-642">Excel and Word</span></span></td> <td><span data-ttu-id="e7222-643">`Office.CoercionType.Matrix`(数组的数组)</span><span class="sxs-lookup"><span data-stu-id="e7222-643">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-644">Access、Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-644">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="e7222-645">`Office.CoercionType.Table`(TableData 对象)</span><span class="sxs-lookup"><span data-stu-id="e7222-645">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-646">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-646">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="e7222-647">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-647">Word</span></span></td> <td><span data-ttu-id="e7222-648">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="e7222-648">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-649">PowerPoint 和 PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="e7222-649">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="e7222-650">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-650">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="e7222-651">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-651">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="e7222-652">要设置的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-652">The data to be set.</span></span> <span data-ttu-id="e7222-653">字符串或[CoercionType](xref:office.Office.CoercionType)值、二维数组或 TableData 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-653">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="e7222-654">如果传递的`data`值为:</span><span class="sxs-lookup"><span data-stu-id="e7222-654">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="e7222-655">一个字符串： 将插入可以强制为 string 的纯文本或任何文本。</span><span class="sxs-lookup"><span data-stu-id="e7222-655">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="e7222-656">在 Excel 中, 还可以将数据指定为有效公式, 将该公式添加到选定的单元格中。</span><span class="sxs-lookup"><span data-stu-id="e7222-656">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="e7222-657">例如，将 data 设置为 "=SUM(A1:A5)" 将计算指定范围中值的总数。</span><span class="sxs-lookup"><span data-stu-id="e7222-657">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="e7222-658">但是，当在绑定单元格中设置公式时，设置后将无法从绑定单元格读取添加的公式（或任何已有公式）。</span><span class="sxs-lookup"><span data-stu-id="e7222-658">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="e7222-659">如果在选定的单元格上调用 Document.getSelectedDataAsync 方法以读取其数据，方法可能仅返回在单元格中显示的数据（即公式的结果）。</span><span class="sxs-lookup"><span data-stu-id="e7222-659">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="e7222-660">数组的数组（“矩阵”）： 将插入不带标题的表数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-660">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="e7222-661">例如, 若要将数据写入两列中的三行, 可以传递如下所示的数组\[ \[: "R1C1"、"R1C2\]" \[、"R2C1"、"R2C2\]" \[、"R3C1"、"R3C2\]\]"。</span><span class="sxs-lookup"><span data-stu-id="e7222-661">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="e7222-662">若要写入包含三行的单个列, 请传递如下所示\[ \[的数组:\]" \[R1C1"、\]" \[R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="e7222-662">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="e7222-663">在 Excel 中, 还可以将数据指定为数组的数组, 其中包含将其添加到选定单元格的有效公式。</span><span class="sxs-lookup"><span data-stu-id="e7222-663">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="e7222-664">例如, 如果不会覆盖任何其他数据, 则将数据\[ \[设置为 "= SUM (A1: a5)", "= AVERAGE (A1: A5\] \] )" 会将这两个公式添加到所选内容中。</span><span class="sxs-lookup"><span data-stu-id="e7222-664">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="e7222-665">与在单个单元格上将公式设置为“text”一样，设置后你将无法读取添加的公式（或任何已有公式），只能读取公式的结果。</span><span class="sxs-lookup"><span data-stu-id="e7222-665">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="e7222-666">TableData 对象： 将插入带标题的表格。</span><span class="sxs-lookup"><span data-stu-id="e7222-666">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="e7222-667">在 excel 中, 如果在传递给 data 参数的 TableData 对象中指定了公式, 则由于 Excel 的 "计算列" 功能 (它会自动在列中重复公式), 因此您可能无法获得预期的结果。</span><span class="sxs-lookup"><span data-stu-id="e7222-667">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="e7222-668">若要在要将包含公式的对象`data`写入选定的表时解决此情况, 请尝试将数据指定为数组的数组 (而不是 TableData 对象), 并将该 coercionType 指定为 "Microsoft. matrix" 或 "matrix"。</span><span class="sxs-lookup"><span data-stu-id="e7222-668">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="e7222-669">但是, 仅当满足以下条件之一时, 此方法才会阻止 "计算列" 功能: (1) 您正在写入列的所有单元格, 或者 (2) 列中已有至少两个不同的公式。</span><span class="sxs-lookup"><span data-stu-id="e7222-669">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: options
          description: <span data-ttu-id="e7222-670">提供有关如何向选定内容中插入数据的选项。</span><span class="sxs-lookup"><span data-stu-id="e7222-670">Provides options for how to insert data to the selection.</span></span>
          type:
            - office.Office.SetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="e7222-671">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-671">Optional.</span></span> <span data-ttu-id="e7222-672">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-672">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-673">.</span><span class="sxs-lookup"><span data-stu-id="e7222-673"></span></span> <span data-ttu-id="e7222-674">AsyncResult 值属性始终返回 undefined, 因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-674">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync_1
    summary: <span data-ttu-id="e7222-675">将指定的数据写入当前所选内容。</span><span class="sxs-lookup"><span data-stu-id="e7222-675">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-676">**要求集**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span><span class="sxs-lookup"><span data-stu-id="e7222-676">**Requirement sets**: [HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)</span></span><!-- --><span data-ttu-id="e7222-677">(使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="e7222-677">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="e7222-678">)、 [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (使用`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="e7222-678">), [ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="e7222-679">)、 [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (使用`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="e7222-679">), [MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="e7222-680">)、 [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (使用`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="e7222-680">), [OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="e7222-681">)、[选择](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span><span class="sxs-lookup"><span data-stu-id="e7222-681">), [Selection](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)</span></span><!-- --><span data-ttu-id="e7222-682">、 [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (使用`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="e7222-682">, [TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="e7222-683">)、 [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (使用`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="e7222-683">), [TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="e7222-684">)</span><span class="sxs-lookup"><span data-stu-id="e7222-684"></span></span>

      <span data-ttu-id="e7222-685">**特定于应用程序的行为**</span><span class="sxs-lookup"><span data-stu-id="e7222-685">**Application-specific behaviors**</span></span>

      <span data-ttu-id="e7222-686">以下特定于应用程序的操作在向选定内容中写入数据时适用。</span><span class="sxs-lookup"><span data-stu-id="e7222-686">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="e7222-687">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-687">Word</span></span></td> <td><span data-ttu-id="e7222-688">如果没有选定内容且插入点位于有效位置, 则在插入点插入指定`data`的</span><span class="sxs-lookup"><span data-stu-id="e7222-688">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="e7222-689">如果`data`是字符串, 则插入指定的文本。</span><span class="sxs-lookup"><span data-stu-id="e7222-689">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-690">如果`data`是数组的数组 ("矩阵") 或 TableData 对象, 则插入一个新的 Word 表。</span><span class="sxs-lookup"><span data-stu-id="e7222-690">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-691">如果`data`为 HTML, 则插入指定的 html。</span><span class="sxs-lookup"><span data-stu-id="e7222-691">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="e7222-692">(**重要说明**: 如果插入的 HTML 中的任何 HTML 无效, 则 Word 不会引发错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-692">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="e7222-693">Word 将尽可能多地插入 HTML, 并忽略任何无效数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-693">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-694">如果`data`是 Office Open xml, 则插入指定的 xml。</span><span class="sxs-lookup"><span data-stu-id="e7222-694">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-695">如果`data`是 base64 编码的图像流, 则插入指定的图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-695">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-696">如果有选定内容</span><span class="sxs-lookup"><span data-stu-id="e7222-696">If there is a selection</span></span></td> <td><span data-ttu-id="e7222-697">它将被替换为与上述`data`相同规则相同的指定项。</span><span class="sxs-lookup"><span data-stu-id="e7222-697">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-698">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-698">Insert images</span></span></td> <td><span data-ttu-id="e7222-699">插入的图像以内嵌方式放置。</span><span class="sxs-lookup"><span data-stu-id="e7222-699">Inserted images are placed inline.</span></span> <span data-ttu-id="e7222-700">imageLeft 和 imageTop 参数将被忽略。</span><span class="sxs-lookup"><span data-stu-id="e7222-700">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="e7222-701">图像的纵横比始终被锁定。</span><span class="sxs-lookup"><span data-stu-id="e7222-701">The image aspect ratio is always locked.</span></span> <span data-ttu-id="e7222-702">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-702">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-703">Excel</span><span class="sxs-lookup"><span data-stu-id="e7222-703">Excel</span></span></td> <td><span data-ttu-id="e7222-704">如果选择了单个单元格</span><span class="sxs-lookup"><span data-stu-id="e7222-704">If a single cell is selected</span></span></td> <td><span data-ttu-id="e7222-705">如果`data`是字符串, 则将指定的文本作为当前单元格的值插入。</span><span class="sxs-lookup"><span data-stu-id="e7222-705">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-706">如果`data`是数组的数组 ("矩阵"), 则插入一组指定的行和列 (如果不会覆盖周围单元格中的其他任何数据)。</span><span class="sxs-lookup"><span data-stu-id="e7222-706">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-707">如果`data`是 TableData 对象, 则插入具有一组指定的行和标题的新 Excel 表 (如果不会覆盖周围单元格中的其他任何数据)。</span><span class="sxs-lookup"><span data-stu-id="e7222-707">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-708">如果选择了多个单元格</span><span class="sxs-lookup"><span data-stu-id="e7222-708">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="e7222-709">, 则会返回错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-709">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="e7222-710">, 则根据中的值更新所选单元格的值`data`</span><span class="sxs-lookup"><span data-stu-id="e7222-710">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="e7222-711">.</span><span class="sxs-lookup"><span data-stu-id="e7222-711"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-712">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-712">Insert images</span></span></td> <td><span data-ttu-id="e7222-713">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-713">Inserted images are floating.</span></span> <span data-ttu-id="e7222-714">imageLeft 和 imageTop 位置参数是相对于当前选定的单元格而言的。</span><span class="sxs-lookup"><span data-stu-id="e7222-714">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="e7222-715">允许 imageLeft 和 imageTop 的值为负数，将由 Excel 重新调整在工作表内放置的图像位置。</span><span class="sxs-lookup"><span data-stu-id="e7222-715">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="e7222-716">除非 imageWidth 和 imageHeight 参数均已提供，否则将锁定图像的纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-716">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="e7222-717">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-717">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="e7222-718">其他所有情况</span><span class="sxs-lookup"><span data-stu-id="e7222-718">All other cases</span></span></td> <td><span data-ttu-id="e7222-719">返回错误。</span><span class="sxs-lookup"><span data-stu-id="e7222-719">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-720">Excel Online</span><span class="sxs-lookup"><span data-stu-id="e7222-720">Excel Online</span></span></td> <td><span data-ttu-id="e7222-721">除了上述 excel 中所述的行为之外, 在 excel Online 中写入数据时也适用这些限制</span><span class="sxs-lookup"><span data-stu-id="e7222-721">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="e7222-722">对此方法的单个调用中, 可以使用`data`参数写入工作表的单元格总数不能超过20000。</span><span class="sxs-lookup"><span data-stu-id="e7222-722">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="e7222-723">传递给`cellFormat`参数的格式组的数量不能超过100。</span><span class="sxs-lookup"><span data-stu-id="e7222-723">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="e7222-724">每个格式设置组由应用于特定单元格范围的一组格式组成。</span><span class="sxs-lookup"><span data-stu-id="e7222-724">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="e7222-725">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="e7222-725">PowerPoint</span></span></td> <td><span data-ttu-id="e7222-726">插入图像</span><span class="sxs-lookup"><span data-stu-id="e7222-726">Insert image</span></span></td> <td><span data-ttu-id="e7222-727">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="e7222-727">Inserted images are floating.</span></span> <span data-ttu-id="e7222-728">position imageLeft 和 imageTop 参数是可选的, 但如果提供, 则应同时存在。</span><span class="sxs-lookup"><span data-stu-id="e7222-728">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="e7222-729">如果提供单个值，则它将被忽略。</span><span class="sxs-lookup"><span data-stu-id="e7222-729">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="e7222-730">允许 imageLeft 和 imageTop 的值为负数，且可以将图像放置在幻灯片的外部。</span><span class="sxs-lookup"><span data-stu-id="e7222-730">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="e7222-731">如果未给定任何可选参数，并且幻灯片有一个占位符，则图像将替换幻灯片中的占位符。</span><span class="sxs-lookup"><span data-stu-id="e7222-731">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="e7222-732">除非 imageWidth和 imageHeight 参数均已提供，否则将锁定图像纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-732">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="e7222-733">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="e7222-733">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="e7222-734">[CoercionType](xref:office.Office.CoercionType)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="e7222-734">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="e7222-735">主机</span><span class="sxs-lookup"><span data-stu-id="e7222-735">Host</span></span></th> <th><span data-ttu-id="e7222-736">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="e7222-736">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="e7222-737">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-737">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="e7222-738">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="e7222-738">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-739">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-739">Excel and Word</span></span></td> <td><span data-ttu-id="e7222-740">`Office.CoercionType.Matrix`(数组的数组)</span><span class="sxs-lookup"><span data-stu-id="e7222-740">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-741">Access、Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-741">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="e7222-742">`Office.CoercionType.Table`(TableData 对象)</span><span class="sxs-lookup"><span data-stu-id="e7222-742">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-743">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-743">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="e7222-744">Word</span><span class="sxs-lookup"><span data-stu-id="e7222-744">Word</span></span></td> <td><span data-ttu-id="e7222-745">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="e7222-745">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="e7222-746">PowerPoint 和 PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="e7222-746">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="e7222-747">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="e7222-747">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="e7222-748">要设置的数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-748">The data to be set.</span></span> <span data-ttu-id="e7222-749">字符串或[CoercionType](xref:office.Office.CoercionType)值、二维数组或 TableData 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-749">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="e7222-750">如果传递的`data`值为:</span><span class="sxs-lookup"><span data-stu-id="e7222-750">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="e7222-751">一个字符串： 将插入可以强制为 string 的纯文本或任何文本。</span><span class="sxs-lookup"><span data-stu-id="e7222-751">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="e7222-752">在 Excel 中, 还可以将数据指定为有效公式, 将该公式添加到选定的单元格中。</span><span class="sxs-lookup"><span data-stu-id="e7222-752">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="e7222-753">例如，将 data 设置为 "=SUM(A1:A5)" 将计算指定范围中值的总数。</span><span class="sxs-lookup"><span data-stu-id="e7222-753">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="e7222-754">但是，当在绑定单元格中设置公式时，设置后将无法从绑定单元格读取添加的公式（或任何已有公式）。</span><span class="sxs-lookup"><span data-stu-id="e7222-754">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="e7222-755">如果在选定的单元格上调用 Document.getSelectedDataAsync 方法以读取其数据，方法可能仅返回在单元格中显示的数据（即公式的结果）。</span><span class="sxs-lookup"><span data-stu-id="e7222-755">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="e7222-756">数组的数组（“矩阵”）： 将插入不带标题的表数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-756">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="e7222-757">例如, 若要将数据写入两列中的三行, 可以传递如下所示的数组\[ \[: "R1C1"、"R1C2\]" \[、"R2C1"、"R2C2\]" \[、"R3C1"、"R3C2\]\]"。</span><span class="sxs-lookup"><span data-stu-id="e7222-757">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="e7222-758">若要写入包含三行的单个列, 请传递如下所示\[ \[的数组:\]" \[R1C1"、\]" \[R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="e7222-758">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="e7222-759">在 Excel 中, 还可以将数据指定为数组的数组, 其中包含将其添加到选定单元格的有效公式。</span><span class="sxs-lookup"><span data-stu-id="e7222-759">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="e7222-760">例如, 如果不会覆盖任何其他数据, 则将数据\[ \[设置为 "= SUM (A1: a5)", "= AVERAGE (A1: A5\] \] )" 会将这两个公式添加到所选内容中。</span><span class="sxs-lookup"><span data-stu-id="e7222-760">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="e7222-761">与在单个单元格上将公式设置为“text”一样，设置后你将无法读取添加的公式（或任何已有公式），只能读取公式的结果。</span><span class="sxs-lookup"><span data-stu-id="e7222-761">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="e7222-762">TableData 对象： 将插入带标题的表格。</span><span class="sxs-lookup"><span data-stu-id="e7222-762">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="e7222-763">在 excel 中, 如果在传递给 data 参数的 TableData 对象中指定了公式, 则由于 Excel 的 "计算列" 功能 (它会自动在列中重复公式), 因此您可能无法获得预期的结果。</span><span class="sxs-lookup"><span data-stu-id="e7222-763">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="e7222-764">若要在要将包含公式的对象`data`写入选定的表时解决此情况, 请尝试将数据指定为数组的数组 (而不是 TableData 对象), 并将该 coercionType 指定为 "Microsoft. matrix" 或 "matrix"。</span><span class="sxs-lookup"><span data-stu-id="e7222-764">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="e7222-765">但是, 仅当满足以下条件之一时, 此方法才会阻止 "计算列" 功能: (1) 您正在写入列的所有单元格, 或者 (2) 列中已有至少两个不同的公式。</span><span class="sxs-lookup"><span data-stu-id="e7222-765">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: callback
          description: '<span data-ttu-id="e7222-766">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-766">Optional.</span></span> <span data-ttu-id="e7222-767">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-767">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-768">.</span><span class="sxs-lookup"><span data-stu-id="e7222-768"></span></span> <span data-ttu-id="e7222-769">AsyncResult 值属性始终返回 undefined, 因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="e7222-769">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync
    summary: |-
      <span data-ttu-id="e7222-770">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-770">Project documents only.</span></span> <span data-ttu-id="e7222-771">为指定的任务标识号设置任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-771">Set task field for specified task Id.</span></span>

      <span data-ttu-id="e7222-772">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-772">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-773">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-773">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-774">任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-774">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="e7222-775">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-775">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="e7222-776">提供一个选项, 用于保留任何类型的未更改的上下文数据, 以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="e7222-776">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="e7222-777">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-777">Optional.</span></span> <span data-ttu-id="e7222-778">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-778">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-779">.</span><span class="sxs-lookup"><span data-stu-id="e7222-779"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync_1
    summary: |-
      <span data-ttu-id="e7222-780">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="e7222-780">Project documents only.</span></span> <span data-ttu-id="e7222-781">为指定的任务标识号设置任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-781">Set task field for specified task Id.</span></span>

      <span data-ttu-id="e7222-782">**重要说明**: 此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="e7222-782">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-783">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-783">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="e7222-784">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="e7222-784">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="e7222-785">任务域。</span><span class="sxs-lookup"><span data-stu-id="e7222-785">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="e7222-786">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="e7222-786">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="e7222-787">可选。</span><span class="sxs-lookup"><span data-stu-id="e7222-787">Optional.</span></span> <span data-ttu-id="e7222-788">回调返回时调用的函数, 其唯一的参数的类型为 " [Office. AsyncResult](xref:office.Office.AsyncResult) "</span><span class="sxs-lookup"><span data-stu-id="e7222-788">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="e7222-789">.</span><span class="sxs-lookup"><span data-stu-id="e7222-789"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.bindings
    summary: <span data-ttu-id="e7222-790">获取提供对文档中定义的绑定的访问的对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-790">Gets an object that provides access to the bindings defined in the document.</span></span>
    remarks: |-
      <span data-ttu-id="e7222-791">您不会直接在脚本中实例化 Document 对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-791">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="e7222-792">若要调用 Document 对象的成员以便与当前文档或工作表交互，请使用脚本中的 `Office.context.document`。</span><span class="sxs-lookup"><span data-stu-id="e7222-792">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="e7222-793">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-793">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - office.Office.Bindings
  - uid: office.Office.Document.customXmlParts
    summary: <span data-ttu-id="e7222-794">获取文档中表示自定义 XML 部件的对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-794">Gets an object that represents the custom XML parts in the document.</span></span>
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - office.Office.CustomXmlParts
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-795">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-795">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.Document.mode
    summary: <span data-ttu-id="e7222-796">获取文档所处的模式。</span><span class="sxs-lookup"><span data-stu-id="e7222-796">Gets the mode the document is in.</span></span>
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - office.Office.DocumentMode
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-797">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-797">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: office.Office.Document.settings
    summary: <span data-ttu-id="e7222-798">获取用于表示当前文档的内容或任务窗格应用程序的已保存自定义设置的对象。</span><span class="sxs-lookup"><span data-stu-id="e7222-798">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - office.Office.Settings
  - uid: office.Office.Document.url
    summary: '<span data-ttu-id="e7222-799">获取主机应用程序当前打开的文档的 URL。</span><span class="sxs-lookup"><span data-stu-id="e7222-799">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="e7222-800">如果 URL 不可用, 则返回 null。</span><span class="sxs-lookup"><span data-stu-id="e7222-800">Returns null if the URL is unavailable.</span></span>'
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="e7222-801">示例</span><span class="sxs-lookup"><span data-stu-id="e7222-801">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```