### YamlMime:UniversalReference
items:
  - uid: 'office!Office.Document:interface'
    summary: <span data-ttu-id="3258b-101">表示与外接程序交互的文档的抽象类。</span><span class="sxs-lookup"><span data-stu-id="3258b-101">An abstract class that represents the document the add-in is interacting with.</span></span>
    remarks: '<span data-ttu-id="3258b-102">**主机**： Excel、PowerPoint、Project、Word</span><span class="sxs-lookup"><span data-stu-id="3258b-102">**Hosts**: Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office!
    children:
      - 'office!Office.Document#addHandlerAsync:member(1)'
      - 'office!Office.Document#addHandlerAsync:member(2)'
      - 'office!Office.Document#bindings:member'
      - 'office!Office.Document#customXmlParts:member'
      - 'office!Office.Document#getActiveViewAsync:member(1)'
      - 'office!Office.Document#getActiveViewAsync:member(2)'
      - 'office!Office.Document#getFileAsync:member(1)'
      - 'office!Office.Document#getFileAsync:member(2)'
      - 'office!Office.Document#getFilePropertiesAsync:member(1)'
      - 'office!Office.Document#getFilePropertiesAsync:member(2)'
      - 'office!Office.Document#getMaxResourceIndexAsync:member(1)'
      - 'office!Office.Document#getMaxResourceIndexAsync:member(2)'
      - 'office!Office.Document#getMaxTaskIndexAsync:member(1)'
      - 'office!Office.Document#getMaxTaskIndexAsync:member(2)'
      - 'office!Office.Document#getProjectFieldAsync:member(1)'
      - 'office!Office.Document#getProjectFieldAsync:member(2)'
      - 'office!Office.Document#getResourceByIndexAsync:member(1)'
      - 'office!Office.Document#getResourceByIndexAsync:member(2)'
      - 'office!Office.Document#getResourceFieldAsync:member(1)'
      - 'office!Office.Document#getResourceFieldAsync:member(2)'
      - 'office!Office.Document#getSelectedDataAsync:member(1)'
      - 'office!Office.Document#getSelectedDataAsync:member(2)'
      - 'office!Office.Document#getSelectedResourceAsync:member(1)'
      - 'office!Office.Document#getSelectedResourceAsync:member(2)'
      - 'office!Office.Document#getSelectedTaskAsync:member(1)'
      - 'office!Office.Document#getSelectedTaskAsync:member(2)'
      - 'office!Office.Document#getSelectedViewAsync:member(1)'
      - 'office!Office.Document#getSelectedViewAsync:member(2)'
      - 'office!Office.Document#getTaskAsync:member(1)'
      - 'office!Office.Document#getTaskAsync:member(2)'
      - 'office!Office.Document#getTaskByIndexAsync:member(1)'
      - 'office!Office.Document#getTaskByIndexAsync:member(2)'
      - 'office!Office.Document#getTaskFieldAsync:member(1)'
      - 'office!Office.Document#getTaskFieldAsync:member(2)'
      - 'office!Office.Document#getWSSUrlAsync:member(1)'
      - 'office!Office.Document#getWSSUrlAsync:member(2)'
      - 'office!Office.Document#goToByIdAsync:member(1)'
      - 'office!Office.Document#goToByIdAsync:member(2)'
      - 'office!Office.Document#mode:member'
      - 'office!Office.Document#removeHandlerAsync:member(1)'
      - 'office!Office.Document#removeHandlerAsync:member(2)'
      - 'office!Office.Document#setResourceFieldAsync:member(1)'
      - 'office!Office.Document#setResourceFieldAsync:member(2)'
      - 'office!Office.Document#setSelectedDataAsync:member(1)'
      - 'office!Office.Document#setSelectedDataAsync:member(2)'
      - 'office!Office.Document#setTaskFieldAsync:member(1)'
      - 'office!Office.Document#setTaskFieldAsync:member(2)'
      - 'office!Office.Document#settings:member'
      - 'office!Office.Document#url:member'
  - uid: 'office!Office.Document#addHandlerAsync:member(1)'
    summary: <span data-ttu-id="3258b-103">为 Document 对象事件添加事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-104">**要求集**： [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="3258b-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="3258b-105">只要每个事件处理程序函数的名称是唯一的，您就可以为指定的事件类型添加多个事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="3258b-106">对于 Document 对象事件，可将事件的参数指定为`Office.EventType.Document.SelectionChanged`或`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="3258b-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="3258b-107">，或此枚举的对应文本值。</span><span class="sxs-lookup"><span data-stu-id="3258b-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - 'office!Office.EventType:enum'
        - id: handler
          description: '<span data-ttu-id="3258b-108">要添加的事件处理程序函数，其唯一的参数的类型为[DocumentSelectionChangedEventArgs](xref:office!Office.DocumentSelectionChangedEventArgs:interface)</span><span class="sxs-lookup"><span data-stu-id="3258b-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office!Office.DocumentSelectionChangedEventArgs:interface)</span></span><!-- --><span data-ttu-id="3258b-109">.</span><span class="sxs-lookup"><span data-stu-id="3258b-109"></span></span> <span data-ttu-id="3258b-110">必需。</span><span class="sxs-lookup"><span data-stu-id="3258b-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="3258b-111">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-112">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-112">Optional.</span></span> <span data-ttu-id="3258b-113">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-114">.</span><span class="sxs-lookup"><span data-stu-id="3258b-114"></span></span>'
          type:
            - 'office!Office.Document#addHandlerAsync~0:complex'
  - uid: 'office!Office.Document#addHandlerAsync:member(2)'
    summary: <span data-ttu-id="3258b-115">为 Document 对象事件添加事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-116">**要求集**： [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="3258b-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="3258b-117">只要每个事件处理程序函数的名称是唯一的，您就可以为指定的事件类型添加多个事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-118">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="3258b-119">对于 Document 对象事件，可将事件的参数指定为`Office.EventType.Document.SelectionChanged`或`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="3258b-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="3258b-120">，或此枚举的对应文本值。</span><span class="sxs-lookup"><span data-stu-id="3258b-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - 'office!Office.EventType:enum'
        - id: handler
          description: '<span data-ttu-id="3258b-121">要添加的事件处理程序函数，其唯一的参数的类型为[DocumentSelectionChangedEventArgs](xref:office!Office.DocumentSelectionChangedEventArgs:interface)</span><span class="sxs-lookup"><span data-stu-id="3258b-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office!Office.DocumentSelectionChangedEventArgs:interface)</span></span><!-- --><span data-ttu-id="3258b-122">.</span><span class="sxs-lookup"><span data-stu-id="3258b-122"></span></span> <span data-ttu-id="3258b-123">必需。</span><span class="sxs-lookup"><span data-stu-id="3258b-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="3258b-124">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-124">Optional.</span></span> <span data-ttu-id="3258b-125">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-126">.</span><span class="sxs-lookup"><span data-stu-id="3258b-126"></span></span>'
          type:
            - 'office!Office.Document#addHandlerAsync~1:complex'
  - uid: 'office!Office.Document#bindings:member'
    summary: <span data-ttu-id="3258b-127">获取提供对文档中定义的绑定的访问的对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-127">Gets an object that provides access to the bindings defined in the document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-128">您不会直接在脚本中实例化 Document 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-128">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="3258b-129">若要调用 Document 对象的成员以便与当前文档或工作表交互，请使用脚本中的 `Office.context.document`。</span><span class="sxs-lookup"><span data-stu-id="3258b-129">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-130">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-130">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - 'office!Office.Bindings:interface'
  - uid: 'office!Office.Document#customXmlParts:member'
    summary: <span data-ttu-id="3258b-131">获取文档中表示自定义 XML 部件的对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-131">Gets an object that represents the custom XML parts in the document.</span></span>
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - 'office!Office.CustomXmlParts:interface'
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-132">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-132">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: 'office!Office.Document#getActiveViewAsync:member(1)'
    summary: <span data-ttu-id="3258b-133">返回演示文稿（编辑或读取）的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="3258b-133">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="3258b-134">**要求集**： [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="3258b-134">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="3258b-135">当视图更改时可以触发事件。</span><span class="sxs-lookup"><span data-stu-id="3258b-135">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-136">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-136">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-137">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-137">Optional.</span></span> <span data-ttu-id="3258b-138">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-138">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-139">.</span><span class="sxs-lookup"><span data-stu-id="3258b-139"></span></span> <span data-ttu-id="3258b-140">结果`value`的属性是演示文稿的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="3258b-140">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="3258b-141">返回的值可以是 "编辑" 或 "读取"。</span><span class="sxs-lookup"><span data-stu-id="3258b-141">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="3258b-142">"编辑" 对应于可在其中编辑幻灯片的任何视图，如 "普通" 或 "大纲视图"。</span><span class="sxs-lookup"><span data-stu-id="3258b-142">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="3258b-143">"读取" 对应于幻灯片放映或阅读视图。</span><span class="sxs-lookup"><span data-stu-id="3258b-143">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - 'office!Office.Document#getActiveViewAsync~0:complex'
  - uid: 'office!Office.Document#getActiveViewAsync:member(2)'
    summary: <span data-ttu-id="3258b-144">返回演示文稿（编辑或读取）的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="3258b-144">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="3258b-145">**要求集**： [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="3258b-145">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="3258b-146">当视图更改时可以触发事件。</span><span class="sxs-lookup"><span data-stu-id="3258b-146">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-147">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-147">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-148">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-148">Optional.</span></span> <span data-ttu-id="3258b-149">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-149">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-150">.</span><span class="sxs-lookup"><span data-stu-id="3258b-150"></span></span> <span data-ttu-id="3258b-151">结果`value`的属性是演示文稿的当前视图的状态。</span><span class="sxs-lookup"><span data-stu-id="3258b-151">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="3258b-152">返回的值可以是 "编辑" 或 "读取"。</span><span class="sxs-lookup"><span data-stu-id="3258b-152">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="3258b-153">"编辑" 对应于可在其中编辑幻灯片的任何视图，如 "普通" 或 "大纲视图"。</span><span class="sxs-lookup"><span data-stu-id="3258b-153">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="3258b-154">"读取" 对应于幻灯片放映或阅读视图。</span><span class="sxs-lookup"><span data-stu-id="3258b-154">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - 'office!Office.Document#getActiveViewAsync~1:complex'
  - uid: 'office!Office.Document#getFileAsync:member(1)'
    summary: '<span data-ttu-id="3258b-155">以高达 4194304 字节 (4 MB) 的切片形式返回整个文档文件。</span><span class="sxs-lookup"><span data-stu-id="3258b-155">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="3258b-156">对于 iOS 上的外接程序，支持最高为65536的文件切片（64 KB）。</span><span class="sxs-lookup"><span data-stu-id="3258b-156">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="3258b-157">请注意，若指定文件切片的大小上限超出允许限制，则会导致一个“内部错误”故障。</span><span class="sxs-lookup"><span data-stu-id="3258b-157">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="3258b-158">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-158">**Requirement sets**:</span></span>

      - <span data-ttu-id="3258b-159">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile)（使用时`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-159">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-160">)</span><span class="sxs-lookup"><span data-stu-id="3258b-160"></span></span>

      - [<span data-ttu-id="3258b-161">文件</span><span class="sxs-lookup"><span data-stu-id="3258b-161">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="3258b-162">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile)（使用时`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-162">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="3258b-163">)</span><span class="sxs-lookup"><span data-stu-id="3258b-163"></span></span>

      <span data-ttu-id="3258b-164">对于在 iOS 上的 Office 之外的 Office 主机应用程序中运行的外接程序，Document.getfileasync 方法支持获取最多为4194304字节（4 MB）的切片中的文件。</span><span class="sxs-lookup"><span data-stu-id="3258b-164">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="3258b-165">对于在 iOS 上运行的 Office 应用程序中运行的外接程序，Document.getfileasync 方法支持获取最高为65536的切片中的文件（64 KB）。</span><span class="sxs-lookup"><span data-stu-id="3258b-165">For add-ins running in Office apps on iOS, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="3258b-166">可以使用 " [Office 文件类型](xref:office!Office.FileType:enum)" 枚举或文本值指定文件类型参数。</span><span class="sxs-lookup"><span data-stu-id="3258b-166">The fileType parameter can be specified by using the [Office.FileType](xref:office!Office.FileType:enum) enumeration or text values.</span></span> <span data-ttu-id="3258b-167">但可能的值因主机而异：</span><span class="sxs-lookup"><span data-stu-id="3258b-167">But the possible values vary with the host:</span></span>

      <span data-ttu-id="3258b-168">在 web 和 Windows 桌面上的 Excel：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-168">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="3258b-169">Mac 上的 Excel：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-169">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-170">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-170"></span></span>

      <span data-ttu-id="3258b-171">PowerPoint 在 web、Windows desktop、Mac 和 iPad 上：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-171">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-172">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-172"></span></span>

      <span data-ttu-id="3258b-173">Word 网页版、Windows desktop、Mac 和 iPad：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-173">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-174">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-174"></span></span><!-- --><span data-ttu-id="3258b-175">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-175"></span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-176">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-176">Examples</span></span>

      ```javascript
      // The following example gets the document in Office Open XML ("compressed") format in 65536 bytes (64 KB) slices.
      // Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.
      function getDocumentAsCompressed() {
          Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, 
              function (result) {
                  if (result.status == "succeeded") {
                  // If the getFileAsync call succeeded, then
                  // result.value will return a valid File Object.
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);

                  // Get the file slices.
                  getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
                  else {
                  app.showNotification("Error:", result.error.message);
                  }
          });
      }

      function getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {
          file.getSliceAsync(nextSlice, function (sliceResult) {
              if (sliceResult.status == "succeeded") {
                  if (!gotAllSlices) { // Failed to get all slices, no need to continue.
                      return;
                  }

                  // Got one slice, store it in a temporary array.
                  // (Or you can do something else, such as
                  // send it to a third-party server.)
                  docdataSlices[sliceResult.value.index] = sliceResult.value.data;
                  if (++slicesReceived == sliceCount) {
                    // All slices have been received.
                    file.closeAsync();
                    onGotAllSlices(docdataSlices);
                  }
                  else {
                      getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
              }
                  else {
                      gotAllSlices = false;
                      file.closeAsync();
                      app.showNotification("getSliceAsync Error:", sliceResult.error.message);
                  }
          });
      }

      function onGotAllSlices(docdataSlices) {
          var docdata = [];
          for (var i = 0; i < docdataSlices.length; i++) {
              docdata = docdata.concat(docdataSlices[i]);
          }

          var fileContent = new String();
          for (var j = 0; j < docdata.length; j++) {
              fileContent += String.fromCharCode(docdata[j]);
          }

          // Now all the file content is stored in 'fileContent' variable,
          // you can do something with it, such as print, fax...
      }

      // The following example gets the document in PDF format.
      Office.context.document.getFileAsync(Office.FileType.Pdf,
          function(result) {
              if (result.status == "succeeded") {
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);
                  // Now, you can call getSliceAsync to download the files,
                  // as described in the previous code segment (compressed format).
                  
                  myFile.closeAsync();
              }
              else {
                  app.showNotification("Error:", result.error.message);
              }
      }
      );
      ```
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="3258b-177">将返回文件的格式</span><span class="sxs-lookup"><span data-stu-id="3258b-177">The format in which the file will be returned</span></span>
          type:
            - 'office!Office.FileType:enum'
        - id: options
          description: <span data-ttu-id="3258b-178">提供用于设置要将文档拆分成的扇区大小的选项。</span><span class="sxs-lookup"><span data-stu-id="3258b-178">Provides options for setting the size of slices that the document will be divided into.</span></span>
          type:
            - 'office!Office.GetFileOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-179">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-179">Optional.</span></span> <span data-ttu-id="3258b-180">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-180">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-181">.</span><span class="sxs-lookup"><span data-stu-id="3258b-181"></span></span> <span data-ttu-id="3258b-182">结果`value`的属性是 File 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-182">The `value` property of the result is the File object.</span></span>'
          type:
            - 'office!Office.Document#getFileAsync~0:complex'
  - uid: 'office!Office.Document#getFileAsync:member(2)'
    summary: '<span data-ttu-id="3258b-183">以高达 4194304 字节 (4 MB) 的切片形式返回整个文档文件。</span><span class="sxs-lookup"><span data-stu-id="3258b-183">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="3258b-184">对于 iOS 上的外接程序，支持最高为65536的文件切片（64 KB）。</span><span class="sxs-lookup"><span data-stu-id="3258b-184">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="3258b-185">请注意，若指定文件切片的大小上限超出允许限制，则会导致一个“内部错误”故障。</span><span class="sxs-lookup"><span data-stu-id="3258b-185">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="3258b-186">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-186">**Requirement sets**:</span></span>

      - <span data-ttu-id="3258b-187">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile)（使用时`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-187">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-188">)</span><span class="sxs-lookup"><span data-stu-id="3258b-188"></span></span>

      - [<span data-ttu-id="3258b-189">文件</span><span class="sxs-lookup"><span data-stu-id="3258b-189">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="3258b-190">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile)（使用时`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-190">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="3258b-191">)</span><span class="sxs-lookup"><span data-stu-id="3258b-191"></span></span>

      <span data-ttu-id="3258b-192">对于在 iOS 上的 Office 之外的 Office 主机应用程序中运行的外接程序，Document.getfileasync 方法支持获取最多为4194304字节（4 MB）的切片中的文件。</span><span class="sxs-lookup"><span data-stu-id="3258b-192">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="3258b-193">对于在 iOS 应用上运行在 Office 中的外接程序，Document.getfileasync 方法支持获取最高为65536的切片中的文件（64 KB）。</span><span class="sxs-lookup"><span data-stu-id="3258b-193">For add-ins running in Office on iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="3258b-194">可以使用 " [Office 文件类型](xref:office!Office.FileType:enum)" 枚举或文本值指定文件类型参数。</span><span class="sxs-lookup"><span data-stu-id="3258b-194">The fileType parameter can be specified by using the [Office.FileType](xref:office!Office.FileType:enum) enumeration or text values.</span></span> <span data-ttu-id="3258b-195">但可能的值因主机而异：</span><span class="sxs-lookup"><span data-stu-id="3258b-195">But the possible values vary with the host:</span></span>

      <span data-ttu-id="3258b-196">在 web 和 Windows 桌面上的 Excel：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-196">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="3258b-197">Mac 上的 Excel：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-197">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-198">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-198"></span></span>

      <span data-ttu-id="3258b-199">PowerPoint 在 web、Windows desktop、Mac 和 iPad 上：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-199">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-200">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-200"></span></span>

      <span data-ttu-id="3258b-201">Word 网页版、Windows desktop、Mac 和 iPad：`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="3258b-201">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="3258b-202">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="3258b-202"></span></span><!-- --><span data-ttu-id="3258b-203">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-203"></span></span>
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="3258b-204">将返回文件的格式</span><span class="sxs-lookup"><span data-stu-id="3258b-204">The format in which the file will be returned</span></span>
          type:
            - 'office!Office.FileType:enum'
        - id: callback
          description: '<span data-ttu-id="3258b-205">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-205">Optional.</span></span> <span data-ttu-id="3258b-206">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-206">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-207">.</span><span class="sxs-lookup"><span data-stu-id="3258b-207"></span></span> <span data-ttu-id="3258b-208">结果`value`的属性是 File 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-208">The `value` property of the result is the File object.</span></span>'
          type:
            - 'office!Office.Document#getFileAsync~1:complex'
  - uid: 'office!Office.Document#getFilePropertiesAsync:member(1)'
    summary: <span data-ttu-id="3258b-209">获取当前文档的文件属性。</span><span class="sxs-lookup"><span data-stu-id="3258b-209">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-210">**要求集**：[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="3258b-210">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="3258b-211">您可以使用 url 属性获取文件的 URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="3258b-211">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="3258b-212">.</span><span class="sxs-lookup"><span data-stu-id="3258b-212"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-213">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-213">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-214">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-214">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-215">.</span><span class="sxs-lookup"><span data-stu-id="3258b-215"></span></span> <span data-ttu-id="3258b-216">结果`value`的属性是文件的属性（URL 位于`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="3258b-216">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="3258b-217">).</span><span class="sxs-lookup"><span data-stu-id="3258b-217"></span></span>'
          type:
            - 'office!Office.Document#getFilePropertiesAsync~0:complex'
  - uid: 'office!Office.Document#getFilePropertiesAsync:member(2)'
    summary: <span data-ttu-id="3258b-218">获取当前文档的文件属性。</span><span class="sxs-lookup"><span data-stu-id="3258b-218">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-219">**要求集**：[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="3258b-219">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="3258b-220">您可以使用 url 属性获取文件的 URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="3258b-220">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="3258b-221">.</span><span class="sxs-lookup"><span data-stu-id="3258b-221"></span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-222">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-222">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-223">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-223">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-224">.</span><span class="sxs-lookup"><span data-stu-id="3258b-224"></span></span> <span data-ttu-id="3258b-225">结果`value`的属性是文件的属性（URL 位于`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="3258b-225">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="3258b-226">).</span><span class="sxs-lookup"><span data-stu-id="3258b-226"></span></span>'
          type:
            - 'office!Office.Document#getFilePropertiesAsync~1:complex'
  - uid: 'office!Office.Document#getMaxResourceIndexAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-227">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-227">Project documents only.</span></span> <span data-ttu-id="3258b-228">获取当前项目中的资源集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="3258b-228">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="3258b-229">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-229">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-230">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-230">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-231">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-231">Optional.</span></span> <span data-ttu-id="3258b-232">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-232">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-233">.</span><span class="sxs-lookup"><span data-stu-id="3258b-233"></span></span> <span data-ttu-id="3258b-234">结果`value`的属性是当前项目的资源集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="3258b-234">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - 'office!Office.Document#getMaxResourceIndexAsync~0:complex'
  - uid: 'office!Office.Document#getMaxResourceIndexAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-235">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-235">Project documents only.</span></span> <span data-ttu-id="3258b-236">获取当前项目中的资源集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="3258b-236">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="3258b-237">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-237">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-238">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-238">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-239">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-239">Optional.</span></span> <span data-ttu-id="3258b-240">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-240">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-241">.</span><span class="sxs-lookup"><span data-stu-id="3258b-241"></span></span> <span data-ttu-id="3258b-242">结果`value`的属性是当前项目的资源集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="3258b-242">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - 'office!Office.Document#getMaxResourceIndexAsync~1:complex'
  - uid: 'office!Office.Document#getMaxTaskIndexAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-243">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-243">Project documents only.</span></span> <span data-ttu-id="3258b-244">获取当前项目中的任务集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="3258b-244">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="3258b-245">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-245">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-246">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-246">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-247">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-247">Optional.</span></span> <span data-ttu-id="3258b-248">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-248">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-249">.</span><span class="sxs-lookup"><span data-stu-id="3258b-249"></span></span> <span data-ttu-id="3258b-250">结果`value`的属性是当前项目的任务集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="3258b-250">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - 'office!Office.Document#getMaxTaskIndexAsync~0:complex'
  - uid: 'office!Office.Document#getMaxTaskIndexAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-251">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-251">Project documents only.</span></span> <span data-ttu-id="3258b-252">获取当前项目中的任务集合的最大索引。</span><span class="sxs-lookup"><span data-stu-id="3258b-252">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="3258b-253">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-253">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-254">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-254">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-255">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-255">Optional.</span></span> <span data-ttu-id="3258b-256">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-256">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-257">.</span><span class="sxs-lookup"><span data-stu-id="3258b-257"></span></span> <span data-ttu-id="3258b-258">结果`value`的属性是当前项目的任务集合中的最大索引号。</span><span class="sxs-lookup"><span data-stu-id="3258b-258">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - 'office!Office.Document#getMaxTaskIndexAsync~1:complex'
  - uid: 'office!Office.Document#getProjectFieldAsync:member(1)'
    summary: <span data-ttu-id="3258b-259">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-259">Project documents only.</span></span> <span data-ttu-id="3258b-260">获取项目域（例如</span><span class="sxs-lookup"><span data-stu-id="3258b-260">Get Project field (Ex.</span></span> <span data-ttu-id="3258b-261">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="3258b-261">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="3258b-262">项目级别字段。</span><span class="sxs-lookup"><span data-stu-id="3258b-262">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="3258b-263">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-263">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-264">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-264">Optional.</span></span> <span data-ttu-id="3258b-265">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-265">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-266">.</span><span class="sxs-lookup"><span data-stu-id="3258b-266"></span></span> <span data-ttu-id="3258b-267">结果`value`的属性包含`fieldValue`属性，它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-267">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - 'office!Office.Document#getProjectFieldAsync~0:complex'
  - uid: 'office!Office.Document#getProjectFieldAsync:member(2)'
    summary: <span data-ttu-id="3258b-268">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-268">Project documents only.</span></span> <span data-ttu-id="3258b-269">获取项目域（例如</span><span class="sxs-lookup"><span data-stu-id="3258b-269">Get Project field (Ex.</span></span> <span data-ttu-id="3258b-270">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="3258b-270">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-271">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-271">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="3258b-272">项目级别字段。</span><span class="sxs-lookup"><span data-stu-id="3258b-272">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="3258b-273">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-273">Optional.</span></span> <span data-ttu-id="3258b-274">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-274">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-275">.</span><span class="sxs-lookup"><span data-stu-id="3258b-275"></span></span> <span data-ttu-id="3258b-276">结果`value`的属性包含`fieldValue`属性，它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-276">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - 'office!Office.Document#getProjectFieldAsync~1:complex'
  - uid: 'office!Office.Document#getResourceByIndexAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-277">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-277">Project documents only.</span></span> <span data-ttu-id="3258b-278">获取资源集合中具有指定索引的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-278">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="3258b-279">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-279">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="3258b-280">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="3258b-280">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="3258b-281">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-281">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-282">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-282">Optional.</span></span> <span data-ttu-id="3258b-283">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-283">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-284">.</span><span class="sxs-lookup"><span data-stu-id="3258b-284"></span></span> <span data-ttu-id="3258b-285">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-285">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getResourceByIndexAsync~0:complex'
  - uid: 'office!Office.Document#getResourceByIndexAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-286">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-286">Project documents only.</span></span> <span data-ttu-id="3258b-287">获取资源集合中具有指定索引的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-287">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="3258b-288">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-288">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-289">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-289">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="3258b-290">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="3258b-290">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="3258b-291">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-291">Optional.</span></span> <span data-ttu-id="3258b-292">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-292">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-293">.</span><span class="sxs-lookup"><span data-stu-id="3258b-293"></span></span> <span data-ttu-id="3258b-294">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-294">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getResourceByIndexAsync~1:complex'
  - uid: 'office!Office.Document#getResourceFieldAsync:member(1)'
    summary: <span data-ttu-id="3258b-295">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-295">Project documents only.</span></span> <span data-ttu-id="3258b-296">获取所提供资源 Id 的资源域。（Ex）</span><span class="sxs-lookup"><span data-stu-id="3258b-296">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="3258b-297">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-297">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-298">资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-298">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="3258b-299">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-299">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-300">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-300">Optional.</span></span> <span data-ttu-id="3258b-301">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-301">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-302">.</span><span class="sxs-lookup"><span data-stu-id="3258b-302"></span></span> <span data-ttu-id="3258b-303">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-303">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getResourceFieldAsync~0:complex'
  - uid: 'office!Office.Document#getResourceFieldAsync:member(2)'
    summary: <span data-ttu-id="3258b-304">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-304">Project documents only.</span></span> <span data-ttu-id="3258b-305">获取所提供资源 Id 的资源域。（Ex）</span><span class="sxs-lookup"><span data-stu-id="3258b-305">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-306">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-306">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="3258b-307">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-307">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-308">资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-308">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="3258b-309">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-309">Optional.</span></span> <span data-ttu-id="3258b-310">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-310">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-311">.</span><span class="sxs-lookup"><span data-stu-id="3258b-311"></span></span> <span data-ttu-id="3258b-312">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-312">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getResourceFieldAsync~1:complex'
  - uid: 'office!Office.Document#getSelectedDataAsync:member(1)'
    summary: <span data-ttu-id="3258b-313">读取包含在文档的当前选择中的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-313">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-314">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-314">**Requirement sets**:</span></span>

      - <span data-ttu-id="3258b-315">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)（使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="3258b-315">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="3258b-316">)</span><span class="sxs-lookup"><span data-stu-id="3258b-316"></span></span>

      - <span data-ttu-id="3258b-317">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)（使用时`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="3258b-317">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="3258b-318">)</span><span class="sxs-lookup"><span data-stu-id="3258b-318"></span></span>

      - <span data-ttu-id="3258b-319">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)（使用时`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="3258b-319">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="3258b-320">)</span><span class="sxs-lookup"><span data-stu-id="3258b-320"></span></span>

      - [<span data-ttu-id="3258b-321">Selection</span><span class="sxs-lookup"><span data-stu-id="3258b-321">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="3258b-322">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)（使用时`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="3258b-322">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="3258b-323">)</span><span class="sxs-lookup"><span data-stu-id="3258b-323"></span></span>

      - <span data-ttu-id="3258b-324">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)（使用时`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-324">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="3258b-325">)</span><span class="sxs-lookup"><span data-stu-id="3258b-325"></span></span>

      <span data-ttu-id="3258b-326">在传递给 getSelectedDataAsync 方法的回调函数中，您可以使用 AsyncResult 对象的属性返回以下信息。</span><span class="sxs-lookup"><span data-stu-id="3258b-326">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-327">属性</span><span class="sxs-lookup"><span data-stu-id="3258b-327">Property</span></span></th> <th><span data-ttu-id="3258b-328">用于...</span><span class="sxs-lookup"><span data-stu-id="3258b-328">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-329">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="3258b-329">AsyncResult.value</span></span></td> <td><span data-ttu-id="3258b-330">始终返回 undefined，这是因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-330">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-331">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="3258b-331">AsyncResult.status</span></span></td> <td><span data-ttu-id="3258b-332">确定操作是成功还是失败。</span><span class="sxs-lookup"><span data-stu-id="3258b-332">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-333">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="3258b-333">AsyncResult.error</span></span></td> <td><span data-ttu-id="3258b-334">如果操作失败，则访问提供错误信息的 Error 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-334">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-335">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="3258b-335">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="3258b-336">在 AsyncResult 对象中未经改动的返回的任何类型的用户定义项。</span><span class="sxs-lookup"><span data-stu-id="3258b-336">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="3258b-337">[CoercionType](xref:office!Office.CoercionType:enum)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="3258b-337">The possible values for the [Office.CoercionType](xref:office!Office.CoercionType:enum) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-338">主机</span><span class="sxs-lookup"><span data-stu-id="3258b-338">Host</span></span></th> <th><span data-ttu-id="3258b-339">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="3258b-339">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-340">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-340">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="3258b-341">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="3258b-341">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-342">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-342">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-343">`Office.CoercionType.Matrix`（数组的数组）</span><span class="sxs-lookup"><span data-stu-id="3258b-343">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-344">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-344">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-345">`Office.CoercionType.Table`（TableData 对象）</span><span class="sxs-lookup"><span data-stu-id="3258b-345">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-346">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-346">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="3258b-347">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-347">Word</span></span></td> <td><span data-ttu-id="3258b-348">`Office.CoercionType.Ooxml`（Office Open XML）</span><span class="sxs-lookup"><span data-stu-id="3258b-348">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-349">PowerPoint 在 web 和 Windows 上</span><span class="sxs-lookup"><span data-stu-id="3258b-349">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="3258b-350">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-350">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="3258b-351">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-351">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="3258b-352">要返回的数据结构的类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-352">The type of data structure to return.</span></span> <span data-ttu-id="3258b-353">有关每个主机支持的强制类型，请参阅 "备注" 部分。</span><span class="sxs-lookup"><span data-stu-id="3258b-353">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - 'office!Office.CoercionType:enum'
        - id: options
          description: <span data-ttu-id="3258b-354">提供用于自定义要返回的数据及其格式的选项。</span><span class="sxs-lookup"><span data-stu-id="3258b-354">Provides options for customizing what data is returned and how it is formatted.</span></span>
          type:
            - 'office!Office.GetSelectedDataOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-355">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-355">Optional.</span></span> <span data-ttu-id="3258b-356">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-356">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-357">.</span><span class="sxs-lookup"><span data-stu-id="3258b-357"></span></span> <span data-ttu-id="3258b-358">结果`value`的属性为当前所选内容中的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-358">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="3258b-359">这是使用 coercionType 参数指定的数据结构或格式返回的。</span><span class="sxs-lookup"><span data-stu-id="3258b-359">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="3258b-360">（请参阅备注，了解有关数据强制的详细信息。）</span><span class="sxs-lookup"><span data-stu-id="3258b-360">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - 'office!Office.Document#getSelectedDataAsync~0:complex'
      typeParameters:
        - id: T
  - uid: 'office!Office.Document#getSelectedDataAsync:member(2)'
    summary: <span data-ttu-id="3258b-361">读取包含在文档的当前选择中的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-361">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-362">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-362">**Requirement sets**:</span></span>

      - <span data-ttu-id="3258b-363">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)（使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="3258b-363">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="3258b-364">)</span><span class="sxs-lookup"><span data-stu-id="3258b-364"></span></span>

      - <span data-ttu-id="3258b-365">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)（使用时`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="3258b-365">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="3258b-366">)</span><span class="sxs-lookup"><span data-stu-id="3258b-366"></span></span>

      - <span data-ttu-id="3258b-367">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)（使用时`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="3258b-367">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="3258b-368">)</span><span class="sxs-lookup"><span data-stu-id="3258b-368"></span></span>

      - [<span data-ttu-id="3258b-369">Selection</span><span class="sxs-lookup"><span data-stu-id="3258b-369">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="3258b-370">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)（使用时`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="3258b-370">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="3258b-371">)</span><span class="sxs-lookup"><span data-stu-id="3258b-371"></span></span>

      - <span data-ttu-id="3258b-372">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)（使用时`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-372">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="3258b-373">)</span><span class="sxs-lookup"><span data-stu-id="3258b-373"></span></span>

      <span data-ttu-id="3258b-374">在传递给 getSelectedDataAsync 方法的回调函数中，您可以使用 AsyncResult 对象的属性返回以下信息。</span><span class="sxs-lookup"><span data-stu-id="3258b-374">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-375">属性</span><span class="sxs-lookup"><span data-stu-id="3258b-375">Property</span></span></th> <th><span data-ttu-id="3258b-376">用于...</span><span class="sxs-lookup"><span data-stu-id="3258b-376">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-377">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="3258b-377">AsyncResult.value</span></span></td> <td><span data-ttu-id="3258b-378">始终返回 undefined，这是因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-378">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-379">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="3258b-379">AsyncResult.status</span></span></td> <td><span data-ttu-id="3258b-380">确定操作是成功还是失败。</span><span class="sxs-lookup"><span data-stu-id="3258b-380">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-381">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="3258b-381">AsyncResult.error</span></span></td> <td><span data-ttu-id="3258b-382">如果操作失败，则访问提供错误信息的 Error 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-382">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-383">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="3258b-383">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="3258b-384">在 AsyncResult 对象中未经改动的返回的任何类型的用户定义项。</span><span class="sxs-lookup"><span data-stu-id="3258b-384">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="3258b-385">[CoercionType](xref:office!Office.CoercionType:enum)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="3258b-385">The possible values for the [Office.CoercionType](xref:office!Office.CoercionType:enum) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-386">主机</span><span class="sxs-lookup"><span data-stu-id="3258b-386">Host</span></span></th> <th><span data-ttu-id="3258b-387">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="3258b-387">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-388">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-388">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="3258b-389">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="3258b-389">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-390">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-390">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-391">`Office.CoercionType.Matrix`（数组的数组）</span><span class="sxs-lookup"><span data-stu-id="3258b-391">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-392">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-392">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-393">`Office.CoercionType.Table`（TableData 对象）</span><span class="sxs-lookup"><span data-stu-id="3258b-393">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-394">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-394">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="3258b-395">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-395">Word</span></span></td> <td><span data-ttu-id="3258b-396">`Office.CoercionType.Ooxml`（Office Open XML）</span><span class="sxs-lookup"><span data-stu-id="3258b-396">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-397">PowerPoint 在 web 和 Windows 上</span><span class="sxs-lookup"><span data-stu-id="3258b-397">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="3258b-398">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-398">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="3258b-399">要返回的数据结构的类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-399">The type of data structure to return.</span></span> <span data-ttu-id="3258b-400">有关每个主机支持的强制类型，请参阅 "备注" 部分。</span><span class="sxs-lookup"><span data-stu-id="3258b-400">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - 'office!Office.CoercionType:enum'
        - id: callback
          description: '<span data-ttu-id="3258b-401">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-401">Optional.</span></span> <span data-ttu-id="3258b-402">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-402">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-403">.</span><span class="sxs-lookup"><span data-stu-id="3258b-403"></span></span> <span data-ttu-id="3258b-404">结果`value`的属性为当前所选内容中的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-404">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="3258b-405">这是使用 coercionType 参数指定的数据结构或格式返回的。</span><span class="sxs-lookup"><span data-stu-id="3258b-405">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="3258b-406">（请参阅备注，了解有关数据强制的详细信息。）</span><span class="sxs-lookup"><span data-stu-id="3258b-406">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - 'office!Office.Document#getSelectedDataAsync~1:complex'
      typeParameters:
        - id: T
  - uid: 'office!Office.Document#getSelectedResourceAsync:member(1)'
    summary: <span data-ttu-id="3258b-407">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-407">Project documents only.</span></span> <span data-ttu-id="3258b-408">获取当前选定的资源的 Id。</span><span class="sxs-lookup"><span data-stu-id="3258b-408">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-409">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-409">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-410">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-410">Optional.</span></span> <span data-ttu-id="3258b-411">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-411">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-412">.</span><span class="sxs-lookup"><span data-stu-id="3258b-412"></span></span> <span data-ttu-id="3258b-413">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-413">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getSelectedResourceAsync~0:complex'
  - uid: 'office!Office.Document#getSelectedResourceAsync:member(2)'
    summary: <span data-ttu-id="3258b-414">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-414">Project documents only.</span></span> <span data-ttu-id="3258b-415">获取当前选定的资源的 Id。</span><span class="sxs-lookup"><span data-stu-id="3258b-415">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-416">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-416">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-417">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-417">Optional.</span></span> <span data-ttu-id="3258b-418">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-418">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-419">.</span><span class="sxs-lookup"><span data-stu-id="3258b-419"></span></span> <span data-ttu-id="3258b-420">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-420">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getSelectedResourceAsync~1:complex'
  - uid: 'office!Office.Document#getSelectedTaskAsync:member(1)'
    summary: <span data-ttu-id="3258b-421">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-421">Project documents only.</span></span> <span data-ttu-id="3258b-422">获取当前所选任务的 Id。</span><span class="sxs-lookup"><span data-stu-id="3258b-422">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-423">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-423">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-424">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-424">Optional.</span></span> <span data-ttu-id="3258b-425">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-425">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-426">.</span><span class="sxs-lookup"><span data-stu-id="3258b-426"></span></span> <span data-ttu-id="3258b-427">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-427">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getSelectedTaskAsync~0:complex'
  - uid: 'office!Office.Document#getSelectedTaskAsync:member(2)'
    summary: <span data-ttu-id="3258b-428">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-428">Project documents only.</span></span> <span data-ttu-id="3258b-429">获取当前所选任务的 Id。</span><span class="sxs-lookup"><span data-stu-id="3258b-429">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-430">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-430">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-431">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-431">Optional.</span></span> <span data-ttu-id="3258b-432">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-432">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-433">.</span><span class="sxs-lookup"><span data-stu-id="3258b-433"></span></span> <span data-ttu-id="3258b-434">结果`value`的属性是作为字符串的资源的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-434">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - 'office!Office.Document#getSelectedTaskAsync~1:complex'
  - uid: 'office!Office.Document#getSelectedViewAsync:member(1)'
    summary: <span data-ttu-id="3258b-435">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-435">Project documents only.</span></span> <span data-ttu-id="3258b-436">获取当前选定的视图类型（Ex）。</span><span class="sxs-lookup"><span data-stu-id="3258b-436">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="3258b-437">甘特图）和视图名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-437">Gantt) and View Name.</span></span>
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-438">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-438">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-439">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-439">Optional.</span></span> <span data-ttu-id="3258b-440">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-440">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-441">.</span><span class="sxs-lookup"><span data-stu-id="3258b-441"></span></span> <span data-ttu-id="3258b-442">结果`value`的属性包含以下属性： `viewName`视图的名称，作为 ProjectViewTypes 常量。</span><span class="sxs-lookup"><span data-stu-id="3258b-442">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="3258b-443">`viewType`-视图的类型，作为 ProjectViewTypes 常量的整数值。</span><span class="sxs-lookup"><span data-stu-id="3258b-443">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - 'office!Office.Document#getSelectedViewAsync~0:complex'
  - uid: 'office!Office.Document#getSelectedViewAsync:member(2)'
    summary: <span data-ttu-id="3258b-444">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-444">Project documents only.</span></span> <span data-ttu-id="3258b-445">获取当前选定的视图类型（Ex）。</span><span class="sxs-lookup"><span data-stu-id="3258b-445">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="3258b-446">甘特图）和视图名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-446">Gantt) and View Name.</span></span>
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-447">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-447">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-448">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-448">Optional.</span></span> <span data-ttu-id="3258b-449">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-449">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-450">.</span><span class="sxs-lookup"><span data-stu-id="3258b-450"></span></span> <span data-ttu-id="3258b-451">结果`value`的属性包含以下属性： `viewName`视图的名称，作为 ProjectViewTypes 常量。</span><span class="sxs-lookup"><span data-stu-id="3258b-451">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="3258b-452">`viewType`-视图的类型，作为 ProjectViewTypes 常量的整数值。</span><span class="sxs-lookup"><span data-stu-id="3258b-452">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - 'office!Office.Document#getSelectedViewAsync~1:complex'
  - uid: 'office!Office.Document#getTaskAsync:member(1)'
    summary: '<span data-ttu-id="3258b-453">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-453">Project documents only.</span></span> <span data-ttu-id="3258b-454">获取给定 taskId 的任务名称、WSS 任务 Id 和 ResourceNames。</span><span class="sxs-lookup"><span data-stu-id="3258b-454">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-455">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-455">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="3258b-456">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-456">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-457">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-457">Optional.</span></span> <span data-ttu-id="3258b-458">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-458">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-459">.</span><span class="sxs-lookup"><span data-stu-id="3258b-459"></span></span> <span data-ttu-id="3258b-460">结果`value`的属性包含以下属性： `taskName` -任务的名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-460">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="3258b-461">`wssTaskId`-同步的 SharePoint 任务列表中任务的 ID。</span><span class="sxs-lookup"><span data-stu-id="3258b-461">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="3258b-462">如果项目未与 SharePoint 任务列表同步，则值为 0。</span><span class="sxs-lookup"><span data-stu-id="3258b-462">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="3258b-463">`resourceNames`-分配给任务的资源的名称的逗号分隔列表。</span><span class="sxs-lookup"><span data-stu-id="3258b-463">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - 'office!Office.Document#getTaskAsync~0:complex'
  - uid: 'office!Office.Document#getTaskAsync:member(2)'
    summary: '<span data-ttu-id="3258b-464">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-464">Project documents only.</span></span> <span data-ttu-id="3258b-465">获取给定 taskId 的任务名称、WSS 任务 Id 和 ResourceNames。</span><span class="sxs-lookup"><span data-stu-id="3258b-465">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-466">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-466">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-467">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-467">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="3258b-468">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-468">Optional.</span></span> <span data-ttu-id="3258b-469">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-469">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-470">.</span><span class="sxs-lookup"><span data-stu-id="3258b-470"></span></span> <span data-ttu-id="3258b-471">结果`value`的属性包含以下属性： `taskName` -任务的名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-471">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="3258b-472">`wssTaskId`-同步的 SharePoint 任务列表中任务的 ID。</span><span class="sxs-lookup"><span data-stu-id="3258b-472">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="3258b-473">如果项目未与 SharePoint 任务列表同步，则值为 0。</span><span class="sxs-lookup"><span data-stu-id="3258b-473">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="3258b-474">`resourceNames`-分配给任务的资源的名称的逗号分隔列表。</span><span class="sxs-lookup"><span data-stu-id="3258b-474">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - 'office!Office.Document#getTaskAsync~1:complex'
  - uid: 'office!Office.Document#getTaskByIndexAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-475">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-475">Project documents only.</span></span> <span data-ttu-id="3258b-476">获取任务集合中具有指定索引的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-476">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="3258b-477">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-477">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="3258b-478">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="3258b-478">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="3258b-479">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-479">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-480">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-480">Optional.</span></span> <span data-ttu-id="3258b-481">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-481">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-482">.</span><span class="sxs-lookup"><span data-stu-id="3258b-482"></span></span> <span data-ttu-id="3258b-483">结果`value`的属性是作为字符串的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-483">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - 'office!Office.Document#getTaskByIndexAsync~0:complex'
  - uid: 'office!Office.Document#getTaskByIndexAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-484">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-484">Project documents only.</span></span> <span data-ttu-id="3258b-485">获取任务集合中具有指定索引的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-485">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="3258b-486">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-486">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-487">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-487">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="3258b-488">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="3258b-488">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="3258b-489">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-489">Optional.</span></span> <span data-ttu-id="3258b-490">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-490">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-491">.</span><span class="sxs-lookup"><span data-stu-id="3258b-491"></span></span> <span data-ttu-id="3258b-492">结果`value`的属性是作为字符串的任务的 GUID。</span><span class="sxs-lookup"><span data-stu-id="3258b-492">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - 'office!Office.Document#getTaskByIndexAsync~1:complex'
  - uid: 'office!Office.Document#getTaskFieldAsync:member(1)'
    summary: <span data-ttu-id="3258b-493">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-493">Project documents only.</span></span> <span data-ttu-id="3258b-494">获取所提供任务 Id 的任务域。（例如，</span><span class="sxs-lookup"><span data-stu-id="3258b-494">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="3258b-495">起始日期）。</span><span class="sxs-lookup"><span data-stu-id="3258b-495">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-496">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-496">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-497">任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-497">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="3258b-498">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-498">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-499">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-499">Optional.</span></span> <span data-ttu-id="3258b-500">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-500">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-501">.</span><span class="sxs-lookup"><span data-stu-id="3258b-501"></span></span> <span data-ttu-id="3258b-502">结果`value`的属性包含`fieldValue`属性，它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-502">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - 'office!Office.Document#getTaskFieldAsync~0:complex'
  - uid: 'office!Office.Document#getTaskFieldAsync:member(2)'
    summary: <span data-ttu-id="3258b-503">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-503">Project documents only.</span></span> <span data-ttu-id="3258b-504">获取所提供任务 Id 的任务域。（例如，</span><span class="sxs-lookup"><span data-stu-id="3258b-504">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="3258b-505">起始日期）。</span><span class="sxs-lookup"><span data-stu-id="3258b-505">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-506">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-506">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-507">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-507">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-508">任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-508">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="3258b-509">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-509">Optional.</span></span> <span data-ttu-id="3258b-510">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-510">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-511">.</span><span class="sxs-lookup"><span data-stu-id="3258b-511"></span></span> <span data-ttu-id="3258b-512">结果`value`的属性包含`fieldValue`属性，它表示指定字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-512">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - 'office!Office.Document#getTaskFieldAsync~1:complex'
  - uid: 'office!Office.Document#getWSSUrlAsync:member(1)'
    summary: '<span data-ttu-id="3258b-513">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-513">Project documents only.</span></span> <span data-ttu-id="3258b-514">获取任务列表的 WSS Url 和列表名称时，MPP 也将同步。</span><span class="sxs-lookup"><span data-stu-id="3258b-514">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="3258b-515">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-515">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-516">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-516">Optional.</span></span> <span data-ttu-id="3258b-517">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-517">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-518">.</span><span class="sxs-lookup"><span data-stu-id="3258b-518"></span></span> <span data-ttu-id="3258b-519">结果`value`的属性包含以下属性： `listName` -同步的 SharePoint 任务列表的名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-519">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="3258b-520">`serverUrl`-同步的 SharePoint 任务列表的 URL。</span><span class="sxs-lookup"><span data-stu-id="3258b-520">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - 'office!Office.Document#getWSSUrlAsync~0:complex'
  - uid: 'office!Office.Document#getWSSUrlAsync:member(2)'
    summary: '<span data-ttu-id="3258b-521">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-521">Project documents only.</span></span> <span data-ttu-id="3258b-522">获取任务列表的 WSS Url 和列表名称时，MPP 也将同步。</span><span class="sxs-lookup"><span data-stu-id="3258b-522">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="3258b-523">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-523">Optional.</span></span> <span data-ttu-id="3258b-524">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-524">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-525">.</span><span class="sxs-lookup"><span data-stu-id="3258b-525"></span></span> <span data-ttu-id="3258b-526">结果`value`的属性包含以下属性： `listName` -同步的 SharePoint 任务列表的名称。</span><span class="sxs-lookup"><span data-stu-id="3258b-526">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="3258b-527">`serverUrl`-同步的 SharePoint 任务列表的 URL。</span><span class="sxs-lookup"><span data-stu-id="3258b-527">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - 'office!Office.Document#getWSSUrlAsync~1:complex'
  - uid: 'office!Office.Document#goToByIdAsync:member(1)'
    summary: <span data-ttu-id="3258b-528">转到文档中指定的对象或位置。</span><span class="sxs-lookup"><span data-stu-id="3258b-528">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-529">**要求集**：[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="3258b-529">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="3258b-530">PowerPoint 不支持母版视图中的 Document.gotobyidasync 方法。</span><span class="sxs-lookup"><span data-stu-id="3258b-530">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="3258b-531">SelectionMode 选项导致的行为因主机而异：</span><span class="sxs-lookup"><span data-stu-id="3258b-531">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="3258b-532">在 Excel 中`Office.SelectionMode.Selected` ：选择绑定或已命名项目中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-532">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="3258b-533">Office.SelectionMode.None 对于文本绑定，选择单元格；对于矩阵绑定、表绑定和已命名的项目，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="3258b-533">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="3258b-534">在 PowerPoint 中`Office.SelectionMode.Selected` ：选择幻灯片标题或幻灯片上的第一个文本框。</span><span class="sxs-lookup"><span data-stu-id="3258b-534">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="3258b-535">`Office.SelectionMode.None`不会选择任何内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-535">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="3258b-536">在 Word 中`Office.SelectionMode.Selected` ：选择绑定中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-536">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="3258b-537">Office.SelectionMode.None 对于文本绑定，将光标移到文本开头；对于矩阵绑定和表绑定，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="3258b-537">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="3258b-538">要转到的对象或位置的标识符。</span><span class="sxs-lookup"><span data-stu-id="3258b-538">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="3258b-539">要转到的位置类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-539">The type of the location to go to.</span></span>
          type:
            - 'office!Office.GoToType:enum'
        - id: options
          description: <span data-ttu-id="3258b-540">提供用于决定是否要选择导航到的位置的选项。</span><span class="sxs-lookup"><span data-stu-id="3258b-540">Provides options for whether to select the location that is navigated to.</span></span>
          type:
            - 'office!Office.GoToByIdOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-541">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-541">Optional.</span></span> <span data-ttu-id="3258b-542">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-542">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-543">.</span><span class="sxs-lookup"><span data-stu-id="3258b-543"></span></span> <span data-ttu-id="3258b-544">结果`value`的属性是当前视图。</span><span class="sxs-lookup"><span data-stu-id="3258b-544">The `value` property of the result is the current view.</span></span>'
          type:
            - 'office!Office.Document#goToByIdAsync~0:complex'
  - uid: 'office!Office.Document#goToByIdAsync:member(2)'
    summary: <span data-ttu-id="3258b-545">转到文档中指定的对象或位置。</span><span class="sxs-lookup"><span data-stu-id="3258b-545">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-546">**要求集**：[不在集合中](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="3258b-546">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="3258b-547">PowerPoint 不支持母版视图中的 Document.gotobyidasync 方法。</span><span class="sxs-lookup"><span data-stu-id="3258b-547">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="3258b-548">SelectionMode 选项导致的行为因主机而异：</span><span class="sxs-lookup"><span data-stu-id="3258b-548">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="3258b-549">在 Excel 中`Office.SelectionMode.Selected` ：选择绑定或已命名项目中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-549">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="3258b-550">Office.SelectionMode.None 对于文本绑定，选择单元格；对于矩阵绑定、表绑定和已命名的项目，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="3258b-550">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="3258b-551">在 PowerPoint 中`Office.SelectionMode.Selected` ：选择幻灯片标题或幻灯片上的第一个文本框。</span><span class="sxs-lookup"><span data-stu-id="3258b-551">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="3258b-552">`Office.SelectionMode.None`不会选择任何内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-552">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="3258b-553">在 Word 中`Office.SelectionMode.Selected` ：选择绑定中的所有内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-553">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="3258b-554">Office.SelectionMode.None 对于文本绑定，将光标移到文本开头；对于矩阵绑定和表绑定，选择第一个数据单元格（不是表格标题行中的第一个单元格）。</span><span class="sxs-lookup"><span data-stu-id="3258b-554">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-555">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-555">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="3258b-556">要转到的对象或位置的标识符。</span><span class="sxs-lookup"><span data-stu-id="3258b-556">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="3258b-557">要转到的位置类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-557">The type of the location to go to.</span></span>
          type:
            - 'office!Office.GoToType:enum'
        - id: callback
          description: '<span data-ttu-id="3258b-558">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-558">Optional.</span></span> <span data-ttu-id="3258b-559">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-559">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-560">.</span><span class="sxs-lookup"><span data-stu-id="3258b-560"></span></span> <span data-ttu-id="3258b-561">结果`value`的属性是当前视图。</span><span class="sxs-lookup"><span data-stu-id="3258b-561">The `value` property of the result is the current view.</span></span>'
          type:
            - 'office!Office.Document#goToByIdAsync~1:complex'
  - uid: 'office!Office.Document#mode:member'
    summary: <span data-ttu-id="3258b-562">获取文档所处的模式。</span><span class="sxs-lookup"><span data-stu-id="3258b-562">Gets the mode the document is in.</span></span>
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - 'office!Office.DocumentMode:enum'
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-563">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-563">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: 'office!Office.Document#removeHandlerAsync:member(1)'
    summary: <span data-ttu-id="3258b-564">删除指定事件类型的事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-564">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="3258b-565">**要求集**： [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="3258b-565">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="3258b-566">事件类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-566">The event type.</span></span> <span data-ttu-id="3258b-567">For document 可以是 "SelectionChanged" 或 "Document.activeviewchanged"。</span><span class="sxs-lookup"><span data-stu-id="3258b-567">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - 'office!Office.EventType:enum'
        - id: options
          description: <span data-ttu-id="3258b-568">提供用于确定要删除的事件处理程序或处理程序的选项。</span><span class="sxs-lookup"><span data-stu-id="3258b-568">Provides options to determine which event handler or handlers are removed.</span></span>
          type:
            - 'office!Office.RemoveHandlerOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-569">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-569">Optional.</span></span> <span data-ttu-id="3258b-570">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-570">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-571">.</span><span class="sxs-lookup"><span data-stu-id="3258b-571"></span></span>'
          type:
            - 'office!Office.Document#removeHandlerAsync~0:complex'
  - uid: 'office!Office.Document#removeHandlerAsync:member(2)'
    summary: <span data-ttu-id="3258b-572">删除指定事件类型的事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="3258b-572">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-573">**要求集**： [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="3258b-573">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="3258b-574">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-574">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="3258b-575">事件类型。</span><span class="sxs-lookup"><span data-stu-id="3258b-575">The event type.</span></span> <span data-ttu-id="3258b-576">For document 可以是 "SelectionChanged" 或 "Document.activeviewchanged"。</span><span class="sxs-lookup"><span data-stu-id="3258b-576">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - 'office!Office.EventType:enum'
        - id: callback
          description: '<span data-ttu-id="3258b-577">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-577">Optional.</span></span> <span data-ttu-id="3258b-578">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-578">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-579">.</span><span class="sxs-lookup"><span data-stu-id="3258b-579"></span></span>'
          type:
            - 'office!Office.Document#removeHandlerAsync~1:complex'
  - uid: 'office!Office.Document#setResourceFieldAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-580">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-580">Project documents only.</span></span> <span data-ttu-id="3258b-581">设置指定资源 Id 的资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-581">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="3258b-582">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-582">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="3258b-583">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-583">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-584">资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-584">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="3258b-585">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-585">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="3258b-586">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-586">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-587">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-587">Optional.</span></span> <span data-ttu-id="3258b-588">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-588">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-589">.</span><span class="sxs-lookup"><span data-stu-id="3258b-589"></span></span>'
          type:
            - 'office!Office.Document#setResourceFieldAsync~0:complex'
  - uid: 'office!Office.Document#setResourceFieldAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-590">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-590">Project documents only.</span></span> <span data-ttu-id="3258b-591">设置指定资源 Id 的资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-591">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="3258b-592">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-592">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-593">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-593">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="3258b-594">或者是一个资源 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-594">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-595">资源域。</span><span class="sxs-lookup"><span data-stu-id="3258b-595">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="3258b-596">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-596">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="3258b-597">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-597">Optional.</span></span> <span data-ttu-id="3258b-598">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-598">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-599">.</span><span class="sxs-lookup"><span data-stu-id="3258b-599"></span></span>'
          type:
            - 'office!Office.Document#setResourceFieldAsync~1:complex'
  - uid: 'office!Office.Document#setSelectedDataAsync:member(1)'
    summary: <span data-ttu-id="3258b-600">将指定的数据写入当前所选内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-600">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-601">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-601">**Requirement sets**:</span></span>

      - [<span data-ttu-id="3258b-602">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="3258b-602">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="3258b-603">（使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="3258b-603">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="3258b-604">)</span><span class="sxs-lookup"><span data-stu-id="3258b-604"></span></span>

      - <span data-ttu-id="3258b-605">[ImageCoercion 1.1](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets)（使用时`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="3258b-605">[ImageCoercion 1.1](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="3258b-606">)</span><span class="sxs-lookup"><span data-stu-id="3258b-606"></span></span>

      - <span data-ttu-id="3258b-607">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)（使用时`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="3258b-607">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="3258b-608">)</span><span class="sxs-lookup"><span data-stu-id="3258b-608"></span></span>

      - <span data-ttu-id="3258b-609">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)（使用时`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="3258b-609">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="3258b-610">)</span><span class="sxs-lookup"><span data-stu-id="3258b-610"></span></span>

      - [<span data-ttu-id="3258b-611">Selection</span><span class="sxs-lookup"><span data-stu-id="3258b-611">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="3258b-612">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)（使用时`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="3258b-612">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="3258b-613">)</span><span class="sxs-lookup"><span data-stu-id="3258b-613"></span></span>

      - <span data-ttu-id="3258b-614">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)（使用时`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-614">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="3258b-615">)</span><span class="sxs-lookup"><span data-stu-id="3258b-615"></span></span>

      - <span data-ttu-id="3258b-616">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12)（使用时`Office.CoercionType.XmlSvg`</span><span class="sxs-lookup"><span data-stu-id="3258b-616">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (when using `Office.CoercionType.XmlSvg`</span></span><!-- --><span data-ttu-id="3258b-617">)</span><span class="sxs-lookup"><span data-stu-id="3258b-617"></span></span>

      <span data-ttu-id="3258b-618">**特定于应用程序的行为**</span><span class="sxs-lookup"><span data-stu-id="3258b-618">**Application-specific behaviors**</span></span>

      <span data-ttu-id="3258b-619">以下特定于应用程序的操作在向选定内容中写入数据时适用。</span><span class="sxs-lookup"><span data-stu-id="3258b-619">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="3258b-620">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-620">Word</span></span></td> <td><span data-ttu-id="3258b-621">如果没有选定内容且插入点位于有效位置，则在插入点插入指定`data`的</span><span class="sxs-lookup"><span data-stu-id="3258b-621">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="3258b-622">如果`data`是字符串，则插入指定的文本。</span><span class="sxs-lookup"><span data-stu-id="3258b-622">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-623">如果`data`是数组的数组（"矩阵"）或 TableData 对象，则插入一个新的 Word 表。</span><span class="sxs-lookup"><span data-stu-id="3258b-623">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-624">如果`data`为 HTML，则插入指定的 html。</span><span class="sxs-lookup"><span data-stu-id="3258b-624">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="3258b-625">（**重要说明**：如果插入的 HTML 中的任何 HTML 无效，则 Word 不会引发错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-625">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="3258b-626">Word 将尽可能多地插入 HTML，并忽略任何无效数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-626">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-627">如果`data`是 OFFICE Open xml，则插入指定的 xml。</span><span class="sxs-lookup"><span data-stu-id="3258b-627">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-628">如果`data`是 base64 编码的图像流，则插入指定的图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-628">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-629">如果有选定内容</span><span class="sxs-lookup"><span data-stu-id="3258b-629">If there is a selection</span></span></td> <td><span data-ttu-id="3258b-630">它将被替换为与上述`data`相同规则相同的指定项。</span><span class="sxs-lookup"><span data-stu-id="3258b-630">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-631">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-631">Insert images</span></span></td> <td><span data-ttu-id="3258b-632">插入的图像以内嵌方式放置。</span><span class="sxs-lookup"><span data-stu-id="3258b-632">Inserted images are placed inline.</span></span> <span data-ttu-id="3258b-633">imageLeft 和 imageTop 参数将被忽略。</span><span class="sxs-lookup"><span data-stu-id="3258b-633">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="3258b-634">图像的纵横比始终被锁定。</span><span class="sxs-lookup"><span data-stu-id="3258b-634">The image aspect ratio is always locked.</span></span> <span data-ttu-id="3258b-635">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-635">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-636">Excel</span><span class="sxs-lookup"><span data-stu-id="3258b-636">Excel</span></span></td> <td><span data-ttu-id="3258b-637">如果选择了单个单元格</span><span class="sxs-lookup"><span data-stu-id="3258b-637">If a single cell is selected</span></span></td> <td><span data-ttu-id="3258b-638">如果`data`是字符串，则将指定的文本作为当前单元格的值插入。</span><span class="sxs-lookup"><span data-stu-id="3258b-638">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-639">如果`data`是数组的数组（"矩阵"），则插入一组指定的行和列（如果不会覆盖周围单元格中的其他任何数据）。</span><span class="sxs-lookup"><span data-stu-id="3258b-639">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-640">如果`data`是 TableData 对象，则插入具有一组指定的行和标题的新 Excel 表（如果不会覆盖周围单元格中的其他任何数据）。</span><span class="sxs-lookup"><span data-stu-id="3258b-640">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-641">如果选择了多个单元格</span><span class="sxs-lookup"><span data-stu-id="3258b-641">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="3258b-642">，则会返回错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-642">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="3258b-643">，则根据中的值更新所选单元格的值`data`</span><span class="sxs-lookup"><span data-stu-id="3258b-643">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="3258b-644">.</span><span class="sxs-lookup"><span data-stu-id="3258b-644"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-645">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-645">Insert images</span></span></td> <td><span data-ttu-id="3258b-646">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-646">Inserted images are floating.</span></span> <span data-ttu-id="3258b-647">imageLeft 和 imageTop 位置参数是相对于当前选定的单元格而言的。</span><span class="sxs-lookup"><span data-stu-id="3258b-647">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="3258b-648">允许 imageLeft 和 imageTop 的值为负数，将由 Excel 重新调整在工作表内放置的图像位置。</span><span class="sxs-lookup"><span data-stu-id="3258b-648">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="3258b-649">除非 imageWidth 和 imageHeight 参数均已提供，否则将锁定图像的纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-649">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="3258b-650">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-650">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-651">其他所有情况</span><span class="sxs-lookup"><span data-stu-id="3258b-651">All other cases</span></span></td> <td><span data-ttu-id="3258b-652">返回错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-652">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-653">在 web 上的 Excel</span><span class="sxs-lookup"><span data-stu-id="3258b-653">Excel on the web</span></span></td> <td><span data-ttu-id="3258b-654">除了上述 Excel 所述的行为之外，在 Excel 网页版中写入数据时也适用这些限制</span><span class="sxs-lookup"><span data-stu-id="3258b-654">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="3258b-655">对此方法的单个调用中，可以使用`data`参数写入工作表的单元格总数不能超过20000。</span><span class="sxs-lookup"><span data-stu-id="3258b-655">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-656">传递给`cellFormat`参数的格式组的数量不能超过100。</span><span class="sxs-lookup"><span data-stu-id="3258b-656">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="3258b-657">每个格式设置组由应用于特定单元格范围的一组格式组成。</span><span class="sxs-lookup"><span data-stu-id="3258b-657">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-658">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="3258b-658">PowerPoint</span></span></td> <td><span data-ttu-id="3258b-659">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-659">Insert image</span></span></td> <td><span data-ttu-id="3258b-660">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-660">Inserted images are floating.</span></span> <span data-ttu-id="3258b-661">Position imageLeft 和 imageTop 参数是可选的，但如果提供，则应同时存在。</span><span class="sxs-lookup"><span data-stu-id="3258b-661">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="3258b-662">如果提供单个值，则它将被忽略。</span><span class="sxs-lookup"><span data-stu-id="3258b-662">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="3258b-663">允许 imageLeft 和 imageTop 的值为负数，且可以将图像放置在幻灯片的外部。</span><span class="sxs-lookup"><span data-stu-id="3258b-663">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="3258b-664">如果未给定任何可选参数，并且幻灯片有一个占位符，则图像将替换幻灯片中的占位符。</span><span class="sxs-lookup"><span data-stu-id="3258b-664">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="3258b-665">除非 imageWidth和 imageHeight 参数均已提供，否则将锁定图像纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-665">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="3258b-666">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-666">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="3258b-667">[CoercionType](xref:office!Office.CoercionType:enum)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="3258b-667">The possible values for the [Office.CoercionType](xref:office!Office.CoercionType:enum) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-668">主机</span><span class="sxs-lookup"><span data-stu-id="3258b-668">Host</span></span></th> <th><span data-ttu-id="3258b-669">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="3258b-669">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-670">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-670">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="3258b-671">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="3258b-671">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-672">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-672">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-673">`Office.CoercionType.Matrix`（数组的数组）</span><span class="sxs-lookup"><span data-stu-id="3258b-673">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-674">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-674">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-675">`Office.CoercionType.Table`（TableData 对象）</span><span class="sxs-lookup"><span data-stu-id="3258b-675">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-676">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-676">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="3258b-677">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-677">Word</span></span></td> <td><span data-ttu-id="3258b-678">`Office.CoercionType.Ooxml`（Office Open XML）</span><span class="sxs-lookup"><span data-stu-id="3258b-678">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-679">PowerPoint 在 web 和 Windows 上</span><span class="sxs-lookup"><span data-stu-id="3258b-679">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="3258b-680">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-680">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="3258b-681">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-681">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="3258b-682">要设置的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-682">The data to be set.</span></span> <span data-ttu-id="3258b-683">字符串或[CoercionType](xref:office!Office.CoercionType:enum)值、二维数组或 TableData 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-683">Either a string or [Office.CoercionType](xref:office!Office.CoercionType:enum) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="3258b-684">如果传递的`data`值为：</span><span class="sxs-lookup"><span data-stu-id="3258b-684">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="3258b-685">一个字符串： 将插入可以强制为 string 的纯文本或任何文本。</span><span class="sxs-lookup"><span data-stu-id="3258b-685">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="3258b-686">在 Excel 中，还可以将数据指定为有效公式，将该公式添加到选定的单元格中。</span><span class="sxs-lookup"><span data-stu-id="3258b-686">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="3258b-687">例如，将 data 设置为 "=SUM(A1:A5)" 将计算指定范围中值的总数。</span><span class="sxs-lookup"><span data-stu-id="3258b-687">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="3258b-688">但是，当在绑定单元格中设置公式时，设置后将无法从绑定单元格读取添加的公式（或任何已有公式）。</span><span class="sxs-lookup"><span data-stu-id="3258b-688">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="3258b-689">如果在选定的单元格上调用 Document.getSelectedDataAsync 方法以读取其数据，方法可能仅返回在单元格中显示的数据（即公式的结果）。</span><span class="sxs-lookup"><span data-stu-id="3258b-689">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="3258b-690">数组的数组（“矩阵”）： 将插入不带标题的表数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-690">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="3258b-691">例如，若要将数据写入两列中的三行，可以传递如下所示的数组\[ \[： "R1C1"、"R1C2\]" \[、"R2C1"、"R2C2\]" \[、"R3C1"、"R3C2\]\]"。</span><span class="sxs-lookup"><span data-stu-id="3258b-691">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="3258b-692">若要写入包含三行的单个列，请传递如下所示\[ \[的数组：\]" \[R1C1"、\]" \[R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="3258b-692">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="3258b-693">在 Excel 中，还可以将数据指定为数组的数组，其中包含将其添加到选定单元格的有效公式。</span><span class="sxs-lookup"><span data-stu-id="3258b-693">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="3258b-694">例如，如果不会覆盖任何其他数据，则将数据\[ \[设置为 "= SUM （A1： a5）"，"= AVERAGE （A1： A5\] \] ）" 会将这两个公式添加到所选内容中。</span><span class="sxs-lookup"><span data-stu-id="3258b-694">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="3258b-695">与在单个单元格上将公式设置为“text”一样，设置后你将无法读取添加的公式（或任何已有公式），只能读取公式的结果。</span><span class="sxs-lookup"><span data-stu-id="3258b-695">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="3258b-696">TableData 对象： 将插入带标题的表格。</span><span class="sxs-lookup"><span data-stu-id="3258b-696">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="3258b-697">在 Excel 中，如果在传递给 data 参数的 TableData 对象中指定了公式，则由于 Excel 的 "计算列" 功能（它会自动在列中重复公式），因此您可能无法获得预期的结果。</span><span class="sxs-lookup"><span data-stu-id="3258b-697">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="3258b-698">若要在要将包含公式的对象`data`写入选定的表时解决此情况，请尝试将数据指定为数组的数组（而不是 TableData 对象），并将该 coercionType 指定为 "Microsoft. matrix" 或 "matrix"。</span><span class="sxs-lookup"><span data-stu-id="3258b-698">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="3258b-699">但是，仅当满足以下条件之一时，此方法才会阻止 "计算列" 功能：（1）您正在写入列的所有单元格，或者（2）列中已有至少两个不同的公式。</span><span class="sxs-lookup"><span data-stu-id="3258b-699">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'office!Office.Document#setSelectedDataAsync~0:complex'
        - id: options
          description: <span data-ttu-id="3258b-700">提供有关如何向选定内容中插入数据的选项。</span><span class="sxs-lookup"><span data-stu-id="3258b-700">Provides options for how to insert data to the selection.</span></span>
          type:
            - 'office!Office.SetSelectedDataOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-701">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-701">Optional.</span></span> <span data-ttu-id="3258b-702">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-702">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-703">.</span><span class="sxs-lookup"><span data-stu-id="3258b-703"></span></span> <span data-ttu-id="3258b-704">AsyncResult 值属性始终返回 undefined，因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-704">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - 'office!Office.Document#setSelectedDataAsync~1:complex'
  - uid: 'office!Office.Document#setSelectedDataAsync:member(2)'
    summary: <span data-ttu-id="3258b-705">将指定的数据写入当前所选内容。</span><span class="sxs-lookup"><span data-stu-id="3258b-705">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="3258b-706">**要求集**：</span><span class="sxs-lookup"><span data-stu-id="3258b-706">**Requirement sets**:</span></span>

      - [<span data-ttu-id="3258b-707">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="3258b-707">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="3258b-708">（使用时`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="3258b-708">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="3258b-709">)</span><span class="sxs-lookup"><span data-stu-id="3258b-709"></span></span>

      - <span data-ttu-id="3258b-710">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets)（使用时`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="3258b-710">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="3258b-711">)</span><span class="sxs-lookup"><span data-stu-id="3258b-711"></span></span>

      - <span data-ttu-id="3258b-712">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)（使用时`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="3258b-712">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="3258b-713">)</span><span class="sxs-lookup"><span data-stu-id="3258b-713"></span></span>

      - <span data-ttu-id="3258b-714">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)（使用时`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="3258b-714">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="3258b-715">)</span><span class="sxs-lookup"><span data-stu-id="3258b-715"></span></span>

      - [<span data-ttu-id="3258b-716">Selection</span><span class="sxs-lookup"><span data-stu-id="3258b-716">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="3258b-717">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)（使用时`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="3258b-717">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="3258b-718">)</span><span class="sxs-lookup"><span data-stu-id="3258b-718"></span></span>

      - <span data-ttu-id="3258b-719">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)（使用时`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="3258b-719">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="3258b-720">)</span><span class="sxs-lookup"><span data-stu-id="3258b-720"></span></span>

      - <span data-ttu-id="3258b-721">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12)（使用时`Office.CoercionType.XmlSvg`</span><span class="sxs-lookup"><span data-stu-id="3258b-721">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (when using `Office.CoercionType.XmlSvg`</span></span><!-- --><span data-ttu-id="3258b-722">)</span><span class="sxs-lookup"><span data-stu-id="3258b-722"></span></span>

      <span data-ttu-id="3258b-723">**特定于应用程序的行为**</span><span class="sxs-lookup"><span data-stu-id="3258b-723">**Application-specific behaviors**</span></span>

      <span data-ttu-id="3258b-724">以下特定于应用程序的操作在向选定内容中写入数据时适用。</span><span class="sxs-lookup"><span data-stu-id="3258b-724">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="3258b-725">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-725">Word</span></span></td> <td><span data-ttu-id="3258b-726">如果没有选定内容且插入点位于有效位置，则在插入点插入指定`data`的</span><span class="sxs-lookup"><span data-stu-id="3258b-726">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="3258b-727">如果`data`是字符串，则插入指定的文本。</span><span class="sxs-lookup"><span data-stu-id="3258b-727">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-728">如果`data`是数组的数组（"矩阵"）或 TableData 对象，则插入一个新的 Word 表。</span><span class="sxs-lookup"><span data-stu-id="3258b-728">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-729">如果`data`为 HTML，则插入指定的 html。</span><span class="sxs-lookup"><span data-stu-id="3258b-729">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="3258b-730">（**重要说明**：如果插入的 HTML 中的任何 HTML 无效，则 Word 不会引发错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-730">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="3258b-731">Word 将尽可能多地插入 HTML，并忽略任何无效数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-731">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-732">如果`data`是 OFFICE Open xml，则插入指定的 xml。</span><span class="sxs-lookup"><span data-stu-id="3258b-732">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-733">如果`data`是 base64 编码的图像流，则插入指定的图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-733">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-734">如果有选定内容</span><span class="sxs-lookup"><span data-stu-id="3258b-734">If there is a selection</span></span></td> <td><span data-ttu-id="3258b-735">它将被替换为与上述`data`相同规则相同的指定项。</span><span class="sxs-lookup"><span data-stu-id="3258b-735">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-736">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-736">Insert images</span></span></td> <td><span data-ttu-id="3258b-737">插入的图像以内嵌方式放置。</span><span class="sxs-lookup"><span data-stu-id="3258b-737">Inserted images are placed inline.</span></span> <span data-ttu-id="3258b-738">imageLeft 和 imageTop 参数将被忽略。</span><span class="sxs-lookup"><span data-stu-id="3258b-738">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="3258b-739">图像的纵横比始终被锁定。</span><span class="sxs-lookup"><span data-stu-id="3258b-739">The image aspect ratio is always locked.</span></span> <span data-ttu-id="3258b-740">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-740">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-741">Excel</span><span class="sxs-lookup"><span data-stu-id="3258b-741">Excel</span></span></td> <td><span data-ttu-id="3258b-742">如果选择了单个单元格</span><span class="sxs-lookup"><span data-stu-id="3258b-742">If a single cell is selected</span></span></td> <td><span data-ttu-id="3258b-743">如果`data`是字符串，则将指定的文本作为当前单元格的值插入。</span><span class="sxs-lookup"><span data-stu-id="3258b-743">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-744">如果`data`是数组的数组（"矩阵"），则插入一组指定的行和列（如果不会覆盖周围单元格中的其他任何数据）。</span><span class="sxs-lookup"><span data-stu-id="3258b-744">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-745">如果`data`是 TableData 对象，则插入具有一组指定的行和标题的新 Excel 表（如果不会覆盖周围单元格中的其他任何数据）。</span><span class="sxs-lookup"><span data-stu-id="3258b-745">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-746">如果选择了多个单元格</span><span class="sxs-lookup"><span data-stu-id="3258b-746">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="3258b-747">，则会返回错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-747">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="3258b-748">，则根据中的值更新所选单元格的值`data`</span><span class="sxs-lookup"><span data-stu-id="3258b-748">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="3258b-749">.</span><span class="sxs-lookup"><span data-stu-id="3258b-749"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-750">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-750">Insert images</span></span></td> <td><span data-ttu-id="3258b-751">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-751">Inserted images are floating.</span></span> <span data-ttu-id="3258b-752">imageLeft 和 imageTop 位置参数是相对于当前选定的单元格而言的。</span><span class="sxs-lookup"><span data-stu-id="3258b-752">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="3258b-753">允许 imageLeft 和 imageTop 的值为负数，将由 Excel 重新调整在工作表内放置的图像位置。</span><span class="sxs-lookup"><span data-stu-id="3258b-753">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="3258b-754">除非 imageWidth 和 imageHeight 参数均已提供，否则将锁定图像的纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-754">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="3258b-755">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-755">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="3258b-756">其他所有情况</span><span class="sxs-lookup"><span data-stu-id="3258b-756">All other cases</span></span></td> <td><span data-ttu-id="3258b-757">返回错误。</span><span class="sxs-lookup"><span data-stu-id="3258b-757">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-758">在 web 上的 Excel</span><span class="sxs-lookup"><span data-stu-id="3258b-758">Excel on the web</span></span></td> <td><span data-ttu-id="3258b-759">除了上述 Excel 所述的行为之外，在 Excel 网页版中写入数据时也适用这些限制</span><span class="sxs-lookup"><span data-stu-id="3258b-759">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="3258b-760">对此方法的单个调用中，可以使用`data`参数写入工作表的单元格总数不能超过20000。</span><span class="sxs-lookup"><span data-stu-id="3258b-760">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="3258b-761">传递给`cellFormat`参数的格式组的数量不能超过100。</span><span class="sxs-lookup"><span data-stu-id="3258b-761">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="3258b-762">每个格式设置组由应用于特定单元格范围的一组格式组成。</span><span class="sxs-lookup"><span data-stu-id="3258b-762">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="3258b-763">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="3258b-763">PowerPoint</span></span></td> <td><span data-ttu-id="3258b-764">插入图像</span><span class="sxs-lookup"><span data-stu-id="3258b-764">Insert image</span></span></td> <td><span data-ttu-id="3258b-765">插入的图像为浮动图像。</span><span class="sxs-lookup"><span data-stu-id="3258b-765">Inserted images are floating.</span></span> <span data-ttu-id="3258b-766">Position imageLeft 和 imageTop 参数是可选的，但如果提供，则应同时存在。</span><span class="sxs-lookup"><span data-stu-id="3258b-766">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="3258b-767">如果提供单个值，则它将被忽略。</span><span class="sxs-lookup"><span data-stu-id="3258b-767">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="3258b-768">允许 imageLeft 和 imageTop 的值为负数，且可以将图像放置在幻灯片的外部。</span><span class="sxs-lookup"><span data-stu-id="3258b-768">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="3258b-769">如果未给定任何可选参数，并且幻灯片有一个占位符，则图像将替换幻灯片中的占位符。</span><span class="sxs-lookup"><span data-stu-id="3258b-769">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="3258b-770">除非 imageWidth和 imageHeight 参数均已提供，否则将锁定图像纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-770">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="3258b-771">如果只给定 imageWidth 参数或只给定 imageHeight 参数，则另一个值将自动扩展以保留原始纵横比。</span><span class="sxs-lookup"><span data-stu-id="3258b-771">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="3258b-772">[CoercionType](xref:office!Office.CoercionType:enum)参数的可能值因主机而异。</span><span class="sxs-lookup"><span data-stu-id="3258b-772">The possible values for the [Office.CoercionType](xref:office!Office.CoercionType:enum) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="3258b-773">主机</span><span class="sxs-lookup"><span data-stu-id="3258b-773">Host</span></span></th> <th><span data-ttu-id="3258b-774">支持的 coercionType</span><span class="sxs-lookup"><span data-stu-id="3258b-774">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="3258b-775">Excel、PowerPoint、Project 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-775">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="3258b-776">`Office.CoercionType.Text`类似</span><span class="sxs-lookup"><span data-stu-id="3258b-776">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-777">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-777">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-778">`Office.CoercionType.Matrix`（数组的数组）</span><span class="sxs-lookup"><span data-stu-id="3258b-778">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-779">Excel 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-779">Excel and Word</span></span></td> <td><span data-ttu-id="3258b-780">`Office.CoercionType.Table`（TableData 对象）</span><span class="sxs-lookup"><span data-stu-id="3258b-780">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-781">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-781">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="3258b-782">Word</span><span class="sxs-lookup"><span data-stu-id="3258b-782">Word</span></span></td> <td><span data-ttu-id="3258b-783">`Office.CoercionType.Ooxml`（Office Open XML）</span><span class="sxs-lookup"><span data-stu-id="3258b-783">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="3258b-784">PowerPoint 在 web 和 Windows 上</span><span class="sxs-lookup"><span data-stu-id="3258b-784">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="3258b-785">Excel、PowerPoint 和 Word</span><span class="sxs-lookup"><span data-stu-id="3258b-785">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="3258b-786">要设置的数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-786">The data to be set.</span></span> <span data-ttu-id="3258b-787">字符串或[CoercionType](xref:office!Office.CoercionType:enum)值、二维数组或 TableData 对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-787">Either a string or [Office.CoercionType](xref:office!Office.CoercionType:enum) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="3258b-788">如果传递的`data`值为：</span><span class="sxs-lookup"><span data-stu-id="3258b-788">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="3258b-789">一个字符串： 将插入可以强制为 string 的纯文本或任何文本。</span><span class="sxs-lookup"><span data-stu-id="3258b-789">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="3258b-790">在 Excel 中，还可以将数据指定为有效公式，将该公式添加到选定的单元格中。</span><span class="sxs-lookup"><span data-stu-id="3258b-790">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="3258b-791">例如，将 data 设置为 "=SUM(A1:A5)" 将计算指定范围中值的总数。</span><span class="sxs-lookup"><span data-stu-id="3258b-791">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="3258b-792">但是，当在绑定单元格中设置公式时，设置后将无法从绑定单元格读取添加的公式（或任何已有公式）。</span><span class="sxs-lookup"><span data-stu-id="3258b-792">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="3258b-793">如果在选定的单元格上调用 Document.getSelectedDataAsync 方法以读取其数据，方法可能仅返回在单元格中显示的数据（即公式的结果）。</span><span class="sxs-lookup"><span data-stu-id="3258b-793">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="3258b-794">数组的数组（“矩阵”）： 将插入不带标题的表数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-794">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="3258b-795">例如，若要将数据写入两列中的三行，可以传递如下所示的数组\[ \[： "R1C1"、"R1C2\]" \[、"R2C1"、"R2C2\]" \[、"R3C1"、"R3C2\]\]"。</span><span class="sxs-lookup"><span data-stu-id="3258b-795">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="3258b-796">若要写入包含三行的单个列，请传递如下所示\[ \[的数组：\]" \[R1C1"、\]" \[R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="3258b-796">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="3258b-797">在 Excel 中，还可以将数据指定为数组的数组，其中包含将其添加到选定单元格的有效公式。</span><span class="sxs-lookup"><span data-stu-id="3258b-797">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="3258b-798">例如，如果不会覆盖任何其他数据，则将数据\[ \[设置为 "= SUM （A1： a5）"，"= AVERAGE （A1： A5\] \] ）" 会将这两个公式添加到所选内容中。</span><span class="sxs-lookup"><span data-stu-id="3258b-798">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="3258b-799">与在单个单元格上将公式设置为“text”一样，设置后你将无法读取添加的公式（或任何已有公式），只能读取公式的结果。</span><span class="sxs-lookup"><span data-stu-id="3258b-799">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="3258b-800">TableData 对象： 将插入带标题的表格。</span><span class="sxs-lookup"><span data-stu-id="3258b-800">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="3258b-801">在 Excel 中，如果在传递给 data 参数的 TableData 对象中指定了公式，则由于 Excel 的 "计算列" 功能（它会自动在列中重复公式），因此您可能无法获得预期的结果。</span><span class="sxs-lookup"><span data-stu-id="3258b-801">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="3258b-802">若要在要将包含公式的对象`data`写入选定的表时解决此情况，请尝试将数据指定为数组的数组（而不是 TableData 对象），并将该 coercionType 指定为 "Microsoft. matrix" 或 "matrix"。</span><span class="sxs-lookup"><span data-stu-id="3258b-802">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="3258b-803">但是，仅当满足以下条件之一时，此方法才会阻止 "计算列" 功能：（1）您正在写入列的所有单元格，或者（2）列中已有至少两个不同的公式。</span><span class="sxs-lookup"><span data-stu-id="3258b-803">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'office!Office.Document#setSelectedDataAsync~2:complex'
        - id: callback
          description: '<span data-ttu-id="3258b-804">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-804">Optional.</span></span> <span data-ttu-id="3258b-805">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-805">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-806">.</span><span class="sxs-lookup"><span data-stu-id="3258b-806"></span></span> <span data-ttu-id="3258b-807">AsyncResult 值属性始终返回 undefined，因为没有要检索的对象或数据。</span><span class="sxs-lookup"><span data-stu-id="3258b-807">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - 'office!Office.Document#setSelectedDataAsync~3:complex'
  - uid: 'office!Office.Document#setTaskFieldAsync:member(1)'
    summary: |-
      <span data-ttu-id="3258b-808">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-808">Project documents only.</span></span> <span data-ttu-id="3258b-809">为指定的任务标识号设置任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-809">Set task field for specified task Id.</span></span>

      <span data-ttu-id="3258b-810">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-810">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-811">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-811">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-812">任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-812">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="3258b-813">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-813">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="3258b-814">提供一个选项，用于保留任何类型的未更改的上下文数据，以供在回调中使用。</span><span class="sxs-lookup"><span data-stu-id="3258b-814">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - 'office!Office.AsyncContextOptions:interface'
        - id: callback
          description: '<span data-ttu-id="3258b-815">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-815">Optional.</span></span> <span data-ttu-id="3258b-816">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-816">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-817">.</span><span class="sxs-lookup"><span data-stu-id="3258b-817"></span></span>'
          type:
            - 'office!Office.Document#setTaskFieldAsync~0:complex'
  - uid: 'office!Office.Document#setTaskFieldAsync:member(2)'
    summary: |-
      <span data-ttu-id="3258b-818">仅限项目文档。</span><span class="sxs-lookup"><span data-stu-id="3258b-818">Project documents only.</span></span> <span data-ttu-id="3258b-819">为指定的任务标识号设置任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-819">Set task field for specified task Id.</span></span>

      <span data-ttu-id="3258b-820">**重要说明**：此 API 仅适用于 Windows 桌面上的 Project 2016。</span><span class="sxs-lookup"><span data-stu-id="3258b-820">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-821">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-821">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="3258b-822">或者是任务 Id 的字符串或值。</span><span class="sxs-lookup"><span data-stu-id="3258b-822">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="3258b-823">任务域。</span><span class="sxs-lookup"><span data-stu-id="3258b-823">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="3258b-824">目标字段的值。</span><span class="sxs-lookup"><span data-stu-id="3258b-824">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="3258b-825">可选。</span><span class="sxs-lookup"><span data-stu-id="3258b-825">Optional.</span></span> <span data-ttu-id="3258b-826">回调返回时调用的函数，其唯一的参数的类型为 " [Office. AsyncResult](xref:office!Office.AsyncResult:interface) "</span><span class="sxs-lookup"><span data-stu-id="3258b-826">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office!Office.AsyncResult:interface)</span></span><!-- --><span data-ttu-id="3258b-827">.</span><span class="sxs-lookup"><span data-stu-id="3258b-827"></span></span>'
          type:
            - 'office!Office.Document#setTaskFieldAsync~1:complex'
  - uid: 'office!Office.Document#settings:member'
    summary: <span data-ttu-id="3258b-828">获取用于表示当前文档的内容或任务窗格应用程序的已保存自定义设置的对象。</span><span class="sxs-lookup"><span data-stu-id="3258b-828">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - 'office!Office.Settings:interface'
  - uid: 'office!Office.Document#url:member'
    summary: <span data-ttu-id="3258b-829">获取主机应用程序当前打开的文档的 URL。</span><span class="sxs-lookup"><span data-stu-id="3258b-829">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="3258b-830">如果 URL 不可用，则返回 null。</span><span class="sxs-lookup"><span data-stu-id="3258b-830">Returns null if the URL is unavailable.</span></span>
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="3258b-831">示例</span><span class="sxs-lookup"><span data-stu-id="3258b-831">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
references:
  - uid: 'office!Office.EventType:enum'
    name: Office.EventType
  - uid: 'office!Office.AsyncContextOptions:interface'
    name: Office.AsyncContextOptions
  - uid: 'office!Office.Document#addHandlerAsync~0:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#addHandlerAsync~1:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Bindings:interface'
    name: Bindings
  - uid: 'office!Office.CustomXmlParts:interface'
    name: CustomXmlParts
  - uid: 'office!Office.Document#getActiveViewAsync~0:complex'
    name: '(result: AsyncResult<"edit" | "read">) => void'
    fullName: '(result: Office.AsyncResult<"edit" | "read">) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <"edit" | "read">) => void
        fullName: <"edit" | "read">) => void
  - uid: 'office!Office.Document#getActiveViewAsync~1:complex'
    name: '(result: AsyncResult<"edit" | "read">) => void'
    fullName: '(result: Office.AsyncResult<"edit" | "read">) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <"edit" | "read">) => void
        fullName: <"edit" | "read">) => void
  - uid: 'office!Office.FileType:enum'
    name: FileType
  - uid: 'office!Office.GetFileOptions:interface'
    name: GetFileOptions
  - uid: 'office!Office.Document#getFileAsync~0:complex'
    name: '(result: AsyncResult<Office.File>) => void'
    fullName: '(result: Office.AsyncResult<Office.File>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <
        fullName: <
      - uid: 'office!Office.File:interface'
        name: Office.File
        fullName: Office.File
      - name: '>) => void'
        fullName: '>) => void'
  - uid: 'office!Office.Document#getFileAsync~1:complex'
    name: '(result: AsyncResult<Office.File>) => void'
    fullName: '(result: Office.AsyncResult<Office.File>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <
        fullName: <
      - uid: 'office!Office.File:interface'
        name: Office.File
        fullName: Office.File
      - name: '>) => void'
        fullName: '>) => void'
  - uid: 'office!Office.Document#getFilePropertiesAsync~0:complex'
    name: '(result: AsyncResult<Office.FileProperties>) => void'
    fullName: '(result: Office.AsyncResult<Office.FileProperties>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <
        fullName: <
      - uid: 'office!Office.FileProperties:interface'
        name: Office.FileProperties
        fullName: Office.FileProperties
      - name: '>) => void'
        fullName: '>) => void'
  - uid: 'office!Office.Document#getFilePropertiesAsync~1:complex'
    name: '(result: AsyncResult<Office.FileProperties>) => void'
    fullName: '(result: Office.AsyncResult<Office.FileProperties>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <
        fullName: <
      - uid: 'office!Office.FileProperties:interface'
        name: Office.FileProperties
        fullName: Office.FileProperties
      - name: '>) => void'
        fullName: '>) => void'
  - uid: 'office!Office.Document#getMaxResourceIndexAsync~0:complex'
    name: '(result: AsyncResult<number>) => void'
    fullName: '(result: Office.AsyncResult<number>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <number>) => void
        fullName: <number>) => void
  - uid: 'office!Office.Document#getMaxResourceIndexAsync~1:complex'
    name: '(result: AsyncResult<number>) => void'
    fullName: '(result: Office.AsyncResult<number>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <number>) => void
        fullName: <number>) => void
  - uid: 'office!Office.Document#getMaxTaskIndexAsync~0:complex'
    name: '(result: AsyncResult<number>) => void'
    fullName: '(result: Office.AsyncResult<number>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <number>) => void
        fullName: <number>) => void
  - uid: 'office!Office.Document#getMaxTaskIndexAsync~1:complex'
    name: '(result: AsyncResult<number>) => void'
    fullName: '(result: Office.AsyncResult<number>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <number>) => void
        fullName: <number>) => void
  - uid: 'office!Office.Document#getProjectFieldAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getProjectFieldAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getResourceByIndexAsync~0:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getResourceByIndexAsync~1:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getResourceFieldAsync~0:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getResourceFieldAsync~1:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.CoercionType:enum'
    name: Office.CoercionType
  - uid: 'office!Office.GetSelectedDataOptions:interface'
    name: GetSelectedDataOptions
  - uid: 'office!Office.Document#getSelectedDataAsync~0:complex'
    name: '(result: AsyncResult<T>) => void'
    fullName: '(result: Office.AsyncResult<T>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <T>) => void
        fullName: <T>) => void
  - uid: 'office!Office.Document#getSelectedDataAsync~1:complex'
    name: '(result: AsyncResult<T>) => void'
    fullName: '(result: Office.AsyncResult<T>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <T>) => void
        fullName: <T>) => void
  - uid: 'office!Office.Document#getSelectedResourceAsync~0:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getSelectedResourceAsync~1:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getSelectedTaskAsync~0:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getSelectedTaskAsync~1:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getSelectedViewAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getSelectedViewAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getTaskAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getTaskAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getTaskByIndexAsync~0:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getTaskByIndexAsync~1:complex'
    name: '(result: AsyncResult<string>) => void'
    fullName: '(result: Office.AsyncResult<string>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <string>) => void
        fullName: <string>) => void
  - uid: 'office!Office.Document#getTaskFieldAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getTaskFieldAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getWSSUrlAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#getWSSUrlAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.GoToType:enum'
    name: GoToType
  - uid: 'office!Office.GoToByIdOptions:interface'
    name: GoToByIdOptions
  - uid: 'office!Office.Document#goToByIdAsync~0:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.Document#goToByIdAsync~1:complex'
    name: '(result: AsyncResult<any>) => void'
    fullName: '(result: Office.AsyncResult<any>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <any>) => void
        fullName: <any>) => void
  - uid: 'office!Office.DocumentMode:enum'
    name: DocumentMode
  - uid: 'office!Office.RemoveHandlerOptions:interface'
    name: RemoveHandlerOptions
  - uid: 'office!Office.Document#removeHandlerAsync~0:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#removeHandlerAsync~1:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setResourceFieldAsync~0:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setResourceFieldAsync~1:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setSelectedDataAsync~0:complex'
    name: 'string | TableData | any[][]'
    fullName: 'string | Office.TableData | any[][]'
    spec.typeScript:
      - name: 'string | '
        fullName: 'string | '
      - uid: 'office!Office.TableData:class'
        name: TableData
        fullName: Office.TableData
      - name: ' | any[][]'
        fullName: ' | any[][]'
  - uid: 'office!Office.SetSelectedDataOptions:interface'
    name: SetSelectedDataOptions
  - uid: 'office!Office.Document#setSelectedDataAsync~1:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setSelectedDataAsync~2:complex'
    name: 'string | TableData | any[][]'
    fullName: 'string | Office.TableData | any[][]'
    spec.typeScript:
      - name: 'string | '
        fullName: 'string | '
      - uid: 'office!Office.TableData:class'
        name: TableData
        fullName: Office.TableData
      - name: ' | any[][]'
        fullName: ' | any[][]'
  - uid: 'office!Office.Document#setSelectedDataAsync~3:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setTaskFieldAsync~0:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Document#setTaskFieldAsync~1:complex'
    name: '(result: AsyncResult<void>) => void'
    fullName: '(result: Office.AsyncResult<void>) => void'
    spec.typeScript:
      - name: '(result: '
        fullName: '(result: '
      - uid: 'office!Office.AsyncResult:interface'
        name: AsyncResult
        fullName: Office.AsyncResult
      - name: <void>) => void
        fullName: <void>) => void
  - uid: 'office!Office.Settings:interface'
    name: Settings